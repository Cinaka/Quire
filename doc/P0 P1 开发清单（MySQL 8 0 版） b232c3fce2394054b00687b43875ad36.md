# P0 / P1 开发清单（MySQL 8.0 版）

<aside>
🗄️

数据库由 PostgreSQL 调整为 **MySQL 8.0**。受影响的点：`JSONB → JSON`、全文检索需 `ngram` 分词器、`UUID` 用 `BINARY(16)` 或 `CHAR(36)`、字符集统一 `utf8mb4/utf8mb4_0900_ai_ci`。详见「MySQL 8.0 适配要点」。

</aside>

## 一、MySQL 8.0 适配要点（先看这个）

| 原方案（PG） | MySQL 8.0 做法 | 说明 |
| --- | --- | --- |
| `JSONB` | `JSON` | MySQL 8 原生 JSON，支持 `JSON_EXTRACT` / `->>`，但**不能直接建索引** |
| JSON 字段索引 | 生成列 + 索引 | `ALTER TABLE t ADD col VARCHAR(64) GENERATED ALWAYS AS (data->>'$.k') STORED, ADD INDEX(col)` |
| 全文检索 `tsvector` | `FULLTEXT`  • `WITH PARSER ngram` | 中文必须用 ngram，`my.cnf` 里设 `ngram_token_size=2` |
| `UUID PK` | `BINARY(16)`（推荐）或 `CHAR(36)` | 用 `UUID_TO_BIN(uuid, 1)` 交换时间位，避免 B+ 树随机写放大 |
| `ENUM` | `ENUM` 或 `TINYINT`  • 应用层枚举 | 推荐 `TINYINT`，改值不用 DDL |
| `TIMESTAMPTZ` | `DATETIME(3)` 存 UTC | MySQL 的 `TIMESTAMP` 有 2038 问题，统一存 UTC 由应用转时区 |
| 部分索引 / `WHERE deleted_at IS NULL` 唯一 | 生成列技巧 | 软删除下的唯一约束需用 `is_deleted` 生成列参与唯一键 |
| 驱动 | `asyncmy`（推荐）或 `aiomysql` | SQLAlchemy 2.0 异步：`mysql+asyncmy://` |

<aside>
⚠️

字符集务必建库时就指定 `CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci`，否则 emoji（心情字段）会写入失败。

</aside>

---

## 二、P0 骨架（预估 3–5 天）

**目标：跑通「前端页面能起来 + 后端能连库 + 一条接口能通」，此阶段不写业务逻辑。**

### P0-1 仓库与工程规范（0.5 天）

- [x]  建仓库（单仓双目录）：`frontend/`（全部前端） + `backend/`（全部后端）
- [ ]  Git 仓库 + `.gitignore` + 分支策略（main / dev / feat-*）
- [ ]  `.editorconfig`、Prettier、ESLint（前端）、Ruff + Black（后端）
- [ ]  pre-commit hook（husky + lint-staged / pre-commit）
- [ ]  `README` 写明本地启动步骤

### P0-2 前端脚手架（1 天）

- [ ]  `pnpm create vite` → Vue3 + TypeScript 模板
- [ ]  安装：`vue-router`、`pinia`、`pinia-plugin-persistedstate`、`axios`、`dayjs`
- [ ]  安装：`tailwindcss`（或 UnoCSS）+ 设计 token（颜色/字号/圆角/间距）
- [ ]  路由骨架：`/` 首页、`/entry/new`、`/entry/:id`、`/list`、`/schedules`、`/settings`
- [ ]  布局组件：移动端底部 TabBar + PC 端侧边栏（响应式断点 768px）
- [ ]  `src/api/request.ts`：axios 实例、baseURL、错误拦截、401 处理占位
- [ ]  Pinia store 骨架：`useUserStore` / `useEntryStore` / `useSettingStore`
- [ ]  环境变量：`.env.development` / `.env.production`

### P0-3 后端脚手架（1 天）

- [ ]  `uv` 或 `poetry` 初始化，Python 3.11+
- [ ]  依赖：`fastapi`、`uvicorn[standard]`、`sqlalchemy[asyncio]>=2.0`、`asyncmy`、`alembic`、`pydantic-settings`、`python-jose`、`passlib[bcrypt]`、`redis`
- [ ]  目录结构：

```jsx
backend/
├─ app/
│  ├─ main.py            # FastAPI 实例、CORS、异常处理
│  ├─ core/              # config / security / deps
│  ├─ models/            # SQLAlchemy ORM
│  ├─ schemas/           # Pydantic v2
│  ├─ api/v1/            # 路由：auth entries schedules tags ...
│  ├─ services/          # 业务逻辑
│  └─ db/                # session / base
├─ alembic/
└─ tests/
```

- [ ]  统一响应结构 `{code, message, data}` + 全局异常处理器
- [ ]  统一分页参数（`page` / `page_size` / `total`）
- [ ]  CORS、请求日志中间件、`/health` 健康检查
- [ ]  Swagger 文档可访问 `/docs`

### P0-4 数据库与迁移（1 天）

- [ ]  **本地裸装 MySQL 8.0**（不使用 Docker，安装与配置详见**第六节**）
- [ ]  MySQL 配置文件写入必填参数：`character-set-server=utf8mb4`、`ngram_token_size=2`、`default-time-zone='+00:00'`、`sql_mode` 含 `STRICT_TRANS_TABLES`，改完**重启服务**并验证生效
- [ ]  Redis **本阶段不安装**：`REDIS_ENABLED=false`，待 P2 做验证码 / 限流 / 签到缓存时再引入
- [ ]  SQLAlchemy 异步 engine + session 依赖注入
- [ ]  Alembic 初始化，配置异步 [env.py](http://env.py)
- [ ]  **建 P1 所需的表**：`diary_entries` / `tags` / `entry_tags` / `media`（`users` 可先建表不启用）
- [ ]  首次迁移 `alembic upgrade head` 通过

### P0-5 联调验收（0.5 天）

- [ ]  前端调通一个 `GET /api/v1/health`，页面显示后端状态
- [ ]  Vite proxy 或 CORS 配置正确，无跨域报错

<aside>
✅

**P0 验收标准**：MySQL 本地服务已启动且 `ngram_token_size = 2` 已生效；后端 `/docs` 可访问；`/api/v1/health` 返回 `mysql: true` 与 `redis: "disabled"`；前端 7 个空白路由可跳转；首页通过 Vite proxy 拿到健康状态且无 CORS 报错。

</aside>

---

## 三、P1 核心闭环（预估 7–10 天）

**目标：不登录就能完整地写日记、看日记、改日记、删日记，数据全在本地 IndexedDB，首页有农历宜忌。这一阶段做完你就可以自己天天用了。**

### P1-1 本地存储层 Dexie（1.5 天）★ 最关键

- [ ]  安装 `dexie` + `dexie-react-hooks` 替代品（Vue 用 `dexie` + `liveQuery` + `useObservable`）
- [ ]  定义 DB schema（版本 1）：

```tsx
db.version(1).stores({
  entries: 'id, entryDate, updatedAt, *tagIds, deletedAt',
  tags: 'id, name',
  media: 'id, entryId',        // Blob 存这里
  schedules: 'id, remindDate, status',
  meta: 'key',                 // 存 lastSyncAt / deviceId 等
})
```

- [ ]  **所有记录客户端生成 UUID v7**（用 `uuid` 包的 v7 或 `nanoid`），禁止自增 ID
- [ ]  每条记录写入 `createdAt` / `updatedAt` / `clientUpdatedAt` / `deletedAt`
- [ ]  封装 Repository 层 `src/db/entryRepo.ts`，**对上层暴露与后端 API 相同的方法签名**（为 P2 无痛切换到云端做准备）
- [ ]  图片以 `Blob` 存 IndexedDB，读取时 `URL.createObjectURL`，组件卸载时 `revokeObjectURL` 防内存泄漏
- [ ]  容量兜底：`navigator.storage.estimate()` 检测配额，超 80% 提示用户

### P1-2 首页：日期与农历宜忌（1.5 天）

- [ ]  安装 `lunar-javascript`
- [ ]  封装 `useLunar(date)` composable，输出：农历年月日、生肖、干支、节气、传统节日、**宜 / 忌**、冲煞、值神
- [ ]  首页头部卡片：公历大字 + 星期 + 农历行
- [ ]  宜忌双栏展示（宜绿 / 忌红），超出折叠「展开更多」
- [ ]  日期切换：左右箭头切换前后一天 + 点击弹出日历选择
- [ ]  今日条目预览列表（读 Dexie，空态插画 + 引导文案）
- [ ]  右下角悬浮「＋」按钮 → 跳 `/entry/new?date=today`
- [ ]  签到按钮**置灰**并标注「登录后可用」（P2 再实现）

### P1-3 添加 / 编辑条目（2.5 天）

- [ ]  集成富文本编辑器 `Tiptap`（StarterKit + Image + Placeholder + CharacterCount）
- [ ]  表单字段：标题、日期选择器、内容、图片、标签、心情、天气
- [ ]  **日期分支逻辑**：
    - [ ]  `= 今天` → 存 `entries`
    - [ ]  `早于今天` → 存 `entries`（补记，UI 标「补记」徽章）
    - [ ]  `&gt; 今天` → 存 `schedules`，`status = pending`，保存后提示「已创建日程提醒，到期后可转为日记」
- [ ]  图片：多选上传 → `browser-image-compression` 压缩（长边 1920，质量 0.8）→ 生成缩略图 → 存 Blob
- [ ]  图片九宫格预览、点击大图、长按/悬停删除、拖拽排序
- [ ]  标签：多选 + 输入即建（本地 `tags` 表去重，忽略大小写与首尾空格）
- [ ]  心情：8 个 emoji 单选；天气：8 个图标单选
- [ ]  **自动草稿**：编辑中每 3 秒 debounce 存 `meta.draft`，意外退出可恢复
- [ ]  离开页面未保存拦截（`onBeforeRouteLeave` + `beforeunload`）
- [ ]  同一天可添加多条，按 `sortOrder` 排序
- [ ]  编辑模式复用同一页面，删除按钮 → 软删除（写 `deletedAt`）

### P1-4 列表页（2 天）

- [ ]  按 年 → 月 → 日 分组渲染，日期分组头吸顶（sticky）
- [ ]  虚拟滚动或按月分页加载（条目多时必做，推荐 `vue-virtual-scroller`）
- [ ]  卡片展示：日期、标题、正文摘要（纯文本截断 80 字）、图片缩略、标签、心情
- [ ]  筛选栏：标签多选、日期区间、有无图片
- [ ]  关键词搜索（本地对 `contentText` 做 `includes`，条目多时上 `flexsearch`）
- [ ]  视图切换按钮：列表视图（本期实现） / 日记本视图（**置灰，标「登录后可用」**）
- [ ]  空态、加载骨架屏、下拉刷新（移动端）

### P1-5 回收站与设置（0.5 天）

- [ ]  设置页：回收站（列出 `deletedAt != null`，支持恢复 / 彻底删除）
- [ ]  设置页：本地数据统计（条目数、占用空间）
- [ ]  设置页：导出 JSON 全量备份（纯前端，`Blob` 下载）— **提前做，建立信任感**
- [ ]  设置页：导入 JSON 恢复

### P1-6 打磨与验收（1 天）

- [ ]  移动端 375px / PC 1440px 双端走查
- [ ]  首屏加载优化：路由懒加载、Tiptap 按需引入
- [ ]  断网测试：飞行模式下全流程可用
- [ ]  边界测试：无图片、超长正文、一天 20 条、图片 10MB、emoji 标题

<aside>
✅

**P1 验收标准**：断网状态下完成「首页看宜忌 → 写一篇带 3 张图和 2 个标签的日记 → 列表页按标签筛到它 → 编辑 → 删除 → 回收站恢复 → 导出 JSON」全流程，刷新浏览器数据不丢。

</aside>

---

## 四、P1 涉及的 MySQL 建表语句（P0 阶段先建好）

<aside>
💡

P1 数据只落本地 IndexedDB，但表结构在 P0 就建好，P2 同步时直接用，避免返工。

</aside>

```sql
CREATE DATABASE quire DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;

CREATE TABLE `users` (
  `id` binary(16) NOT NULL,
  `phone` varchar(20) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci DEFAULT NULL COMMENT '手机号',
  `email` varchar(128) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci DEFAULT NULL COMMENT '邮箱',
  `password_hash` varchar(255) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci DEFAULT NULL COMMENT '密码',
  `nickname` varchar(64) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci DEFAULT NULL COMMENT '昵称',
  `avatar` varchar(512) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci DEFAULT NULL COMMENT '头像',
  `timezone` varchar(64) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci NOT NULL DEFAULT 'Asia/Shanghai' COMMENT '时区',
  `created_at` datetime(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updated_at` datetime(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_phone` (`phone`),
  UNIQUE KEY `uk_email` (`email`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;

CREATE TABLE diary_entries (
  id                BINARY(16)  NOT NULL PRIMARY KEY,
  user_id           BINARY(16)  NOT NULL,
  entry_date        DATE        NOT NULL,
  sort_order        INT         NOT NULL DEFAULT 0,
  title             VARCHAR(255) NULL,
  content           JSON        NULL,          -- Tiptap JSON
  content_text      MEDIUMTEXT  NULL,          -- 纯文本，供全文检索
  mood              VARCHAR(16) NULL,
  weather           VARCHAR(16) NULL,
  from_schedule_id  BINARY(16)  NULL,          -- 幂等关键
  client_updated_at DATETIME(3) NULL,
  created_at        DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updated_at        DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  deleted_at        DATETIME(3) NULL,
  UNIQUE KEY uk_from_schedule (from_schedule_id),
  KEY idx_user_date (user_id, entry_date DESC),
  KEY idx_user_updated (user_id, updated_at),
  FULLTEXT KEY ft_content (title, content_text) WITH PARSER ngram
) ENGINE=InnoDB;

CREATE TABLE tags (
  id         BINARY(16)  NOT NULL PRIMARY KEY,
  user_id    BINARY(16)  NOT NULL,
  name       VARCHAR(32) NOT NULL,
  color      VARCHAR(16) NULL,
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  UNIQUE KEY uk_user_name (user_id, name)
) ENGINE=InnoDB;

CREATE TABLE entry_tags (
  entry_id BINARY(16) NOT NULL,
  tag_id   BINARY(16) NOT NULL,
  PRIMARY KEY (entry_id, tag_id),
  KEY idx_tag (tag_id)
) ENGINE=InnoDB;

CREATE TABLE media (
  id         BINARY(16)   NOT NULL PRIMARY KEY,
  user_id    BINARY(16)   NOT NULL,
  entry_id   BINARY(16)   NULL,
  url        VARCHAR(512) NOT NULL,
  thumb_url  VARCHAR(512) NULL,
  width      INT NULL,
  height     INT NULL,
  size       BIGINT NULL,
  sort_order INT NOT NULL DEFAULT 0,
  created_at DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  KEY idx_entry (entry_id)
) ENGINE=InnoDB;
```

**中文全文检索用法：**

```sql
SELECT * FROM diary_entries
WHERE MATCH(title, content_text) AGAINST('旅行' IN BOOLEAN MODE)
  AND user_id = ? AND deleted_at IS NULL;
```

---

## 五、风险与注意事项

1. **Repository 抽象一定要在 P1 做**。如果 P1 直接在组件里写 `db.entries.add()`，P2 接云端时要改几十处，成本翻倍。
2. **UUID v7 而非 v4**。v7 带时间戳前缀，配合 `UUID_TO_BIN(x, 1)` 写入 InnoDB 聚簇索引接近顺序写，v4 会导致严重页分裂。
3. **IndexedDB Blob 内存泄漏**。列表页大量 `createObjectURL` 不释放会让移动端浏览器崩溃，务必在组件卸载时 revoke。
4. **ngram_token_size 改了要重建索引**。这个参数只能在 `my.cnf` 设置且需重启，改完已有 FULLTEXT 索引要 `OPTIMIZE TABLE` 重建，P0 阶段就定好。
5. **软删除下的唯一约束**。`tags` 表若要支持删除后重建同名标签，需把 `deleted_at` 转成生成列参与唯一键，否则会撞唯一约束。
6. **Tiptap JSON 结构要版本化**。`content` JSON 里存一个 `schemaVersion`，未来改编辑器时好做数据迁移。
7. **P1 不要碰登录**。一旦开始做账号，游客态的边界会被打破，容易返工。严格守住「P1 只做本地」。
8. **不用 Docker 后的唯一风险点**是环境参数靠人工保证。`ngram_token_size` 配错时**不报错**，只是中文搜不到，排查成本极高。已在第六节附上部署参数核对清单，上线前跑一次 SQL 即可。

---

## 六、本地环境搭建（不使用 Docker）

<aside>
🛠️

本项目**开发与部署均不使用 Docker**，MySQL 裸装在宿主机。代码层面无任何差异，唯一需额外关注的是**配置参数靠人工核对**。

</aside>

### 1. MySQL 8.0 安装

| 系统 | 安装方式 | 配置文件位置 |
| --- | --- | --- |
| Windows | MySQL Installer（选 Server + Workbench） | `C:\ProgramData\MySQL\MySQL Server 8.0\my.ini` |
| macOS | `brew install mysql@8.0` → `brew services start mysql@8.0` | `/opt/homebrew/etc/my.cnf`（Apple Silicon） |
| Linux | apt / yum 安装 `mysql-server-8.0` | `/etc/mysql/my.cnf` 或 `/etc/my.cnf` |

### 2. 必填配置（写入配置文件后重启服务）

```
[mysqld]
character-set-server = utf8mb4
collation-server = utf8mb4_0900_ai_ci
default-time-zone = '+00:00'
sql_mode = STRICT_TRANS_TABLES,NO_ENGINE_SUBSTITUTION,ERROR_FOR_DIVISION_BY_ZERO
ngram_token_size = 2
innodb_ft_min_token_size = 1
max_connections = 300
lower_case_table_names = 1

[client]
default-character-set = utf8mb4

[mysql]
default-character-set = utf8mb4
```

重启服务：

```bash
# Windows（管理员 PowerShell）
net stop MySQL80 ; net start MySQL80

# macOS
brew services restart mysql@8.0

# Linux
sudo systemctl restart mysqld
```

### 3. 建库与建用户

```sql
CREATE DATABASE qingjian DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
CREATE USER 'qingjian'@'localhost' IDENTIFIED BY '自定义强密码';
GRANT ALL PRIVILEGES ON qingjian.* TO 'qingjian'@'localhost';
FLUSH PRIVILEGES;
```

### 4. Redis：P0 / P1 不安装

Redis 在本方案中只服务于**验证码、登录限流、签到缓存**，全是 **P2 及以后**的事。因此：

- `config.py` 增加 `REDIS_ENABLED: bool = False`
- 健康检查接口在未启用时返回 `"redis": "disabled"`，不影响验收
- 待 P2 再安装（Windows 上建议用 WSL2 或 Memurai，官方 Redis 早已不维护 Windows 版）

### 5. 部署参数核对清单（上线前必跑）

<aside>
⚠️

`ngram_token_size` 配错时**不会报错**，只是中文全文检索静默失效。且该参数修改后，**已建的 FULLTEXT 索引必须重建**（`OPTIMIZE TABLE diary_entries;`），否则旧索引仍按旧分词长度工作。

</aside>

```sql
-- 新环境部署后第一件事：跑这段，逐项对照期望值
SHOW VARIABLES LIKE 'ngram_token_size';      -- 期望 2
SHOW VARIABLES LIKE 'character_set_server';  -- 期望 utf8mb4
SHOW VARIABLES LIKE 'collation_server';      -- 期望 utf8mb4_0900_ai_ci
SELECT @@global.time_zone;                   -- 期望 +00:00
SELECT @@global.sql_mode;                    -- 必含 STRICT_TRANS_TABLES
SELECT VERSION();                            -- 期望 8.0.x

-- 建表后验证中文全文检索真的能用（插一条测试数据后执行）
SELECT COUNT(*) FROM diary_entries
WHERE MATCH(title, content_text) AGAINST('旅行' IN BOOLEAN MODE);
-- 返回 0 但数据确实包含「旅行」→ ngram 未生效，回去查配置
```

除此之外，手动部署还需确认：

- [ ]  生产库用独立账号，**非 root**，且只授权单库
- [ ]  `.env` 中 `JWT_SECRET` 已换成随机长字符串，`DEBUG=false`
- [ ]  数据库定时备份（`mysqldump` + crontab），并实际测过一次恢复
- [ ]  MySQL 不对公网监听（`bind-address = 127.0.0.1`）
- [ ]  Nginx 反代 + HTTPS，前端静态资源开 gzip / brotli

---

## 七、多端差异化架构约定

### 1. 实际是三个平台，不是两个

| 平台 | 定位 | 代码关系 |
| --- | --- | --- |
| PC 浏览器 | 核心场景：日记本视图、打印、导出 | 与手机浏览器**同一套代码**（响应式） |
| 手机浏览器 / PWA | 随手记录 | 同上 |
| 微信小程序（二期） | 引流与轻量场景 | **不复用 UI**，只复用逻辑 |

小程序不能调起打印机、无 DOM（Tiptap 无法运行）、PDF 导出受限、自定义字体受限——三个核心卖点全废，因此**共用的是逻辑，不是界面**。

```jsx
┌────────────────────────────────────┐
│  shared  ← 纯 TS，零 UI，零平台 API             │
│  · 农历宜忌封装     · 日期分支判定规则        │
│  · 类型定义         · 内容格式转换           │
│  · API 契约类型     · 连续天数 / 校验规则      │
└──────┬──────────────────────────┬────────┘
       │                              │
┌──────┴────────────┐   ┌────────┴─────────┐
│ frontend（Vue3）     │   │ 小程序（二期）       │
│ PC + 移动浏览器     │   │ 只做签到 + 速记 +   │
│ 全功能响应式       │   │ 浏览，做减法         │
└──────────────────┘   └─────────────────┘
```

### 2. PC 与移动的差异处理：三级递进

**能用第 1 级就绝不用第 3 级。**

| 级别 | 手法 | 覆盖比例 | 说明 |
| --- | --- | --- | --- |
| 第 1 级 | 纯 CSS 断点（Tailwind `md:`） | ~80% | 布局、栏数、间距、TabBar / 侧边栏互斥 |
| 第 2 级 | 逻辑与呈现分离 | ~15% | 结构差异大时，拆 `XxxDesktop.vue` / `XxxMobile.vue`，共用同一个 composable |
| 第 3 级 | 平台能力适配层 | ~5% | 打印、下载、字体加载、剪贴板，全部收进 `capabilities/` |

**禁止事项**：不要在单个组件里用 `v-if` 并列两套完整模板；不要在业务组件里直接调 `window.print()` 或 `document.createElement`。

第 2 级的正确形状：

```tsx
// composables/useHome.ts —— 逻辑唯一来源，两端共用
export function useHome() {
  const date = ref(dayjs())
  const lunar = computed(() => getLunarInfo(date.value)) // 来自 shared
  const entries = ref<Entry[]>([])
  async function load() {
    entries.value = await entryRepo.listByDate(date.value.format("YYYY-MM-DD"))
  }
  return { date, lunar, entries, load }
}
```

```
<!-- views/HomeView.vue —— 只做分发，不含业务逻辑 -->
<script setup lang="ts">
import { defineAsyncComponent } from "vue"
import { useBreakpoint } from "@/composables/useBreakpoint"
const { isDesktop } = useBreakpoint()
const Desktop = defineAsyncComponent(() => import("./home/HomeDesktop.vue"))
const Mobile = defineAsyncComponent(() => import("./home/HomeMobile.vue"))
</script>

<template>
  <component :is="isDesktop ? Desktop : Mobile" />
</template>
```

第 3 级的能力适配层：

```tsx
// capabilities/index.ts
export interface Capabilities {
  canPrint: boolean
  canExportPdf: boolean
  print(): Promise<void>
  saveFile(blob: Blob, filename: string): Promise<void>
}
```

业务代码只写 `if (caps.canPrint)`，UI 自动降级（按钮置灰或隐藏）。将来上小程序只需提供一份 `mpCapabilities` 实现，业务层零改动。

### 3. 路由差异

移动端「列表 → 详情」是两个页面，PC 上应为同屏双栏。保持路由结构一致（保证链接可分享），在 PC 上把 `/entry/:id` 作为 `/list` 的子路由渲染到右栏即可。

### 4. 内容存储格式（现在必须定下来）

<aside>
🔑

这是唯一一个「现在不想清楚，将来必然返工」的点。

</aside>

| 方案 | PC 体验 | 小程序可行性 | 结论 |
| --- | --- | --- | --- |
| Tiptap JSON | ⭐⭐⭐⭐⭐ | 需自写 JSON → 小程序节点渲染器（2–3 天） | ✅ **采用** |
| Markdown | ⭐⭐⭐⭐ | `towxml` 直接渲染，几乎零成本 | 备选 |
| HTML 字符串 | ⭐⭐⭐ | `rich-text` 标签白名单坑多 | ❌ |

采用 Tiptap JSON，但**从第一天起就在 `shared` 里写好三个纯函数**，把格式转换与平台彻底解耦：

```tsx
// shared/content.ts
export function toPlainText(doc: TiptapDoc): string  // 存 content_text，供全文检索与列表摘要
export function toMarkdown(doc: TiptapDoc): string   // 供导出、小程序渲染
export function fromMarkdown(md: string): TiptapDoc  // 供导入、小程序速记回写
```

小程序端的功能边界：**只读渲染走 Markdown，编辑只支持纯文本速记**（回写时 `fromMarkdown`）。这个边界诚实且足够——用户在小程序里本来也不会写长文。

### 5. P1 阶段需要留的三个口子

**不要为了将来的小程序现在上 uni-app，也不要现在建 monorepo 分包。**只需在 P1 留好三个口子，成本几乎为零；等 P5、P6 做完再补，成本是重写。

- [ ]  **`src/shared/`** 目录（将来平移为独立包）：农历封装、日期分支判定、内容格式转换、类型定义。**里面禁止出现 `window`、`document`、Vue API**
- [ ]  **`src/capabilities/`** 能力适配层：打印、下载、字体加载全走这里
- [ ]  **`useXxx()` composable 承载业务逻辑**，组件只管呈现（与 P1-1 的 Repository 抽象同一思路：让平台相关的东西可替换）