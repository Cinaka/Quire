# P1-6 打磨收尾源码

<aside>
📌

**2026-09-23 补充：上名青简的阶段边界。**

「上名青简」语义仍是每日签到，完整功能属于 P3，不在 P2 伪造签到写入。P2 中未登录用户看到「上名青简 / 登录后可用」，点击进入云笺登录；登录后该占位入口隐藏。P3 实现签到后，再替换为真正的签到状态与操作。

</aside>

P1-6 分九个批次（A–I），拆分表与排序理由在[青简 / Quire 项目状态索引（真相来源）](https://app.notion.com/p/Quire-eb64c86897b44b3ca979c923e3984bd9?pvs=21)第三节。这一页只放**实际落地的源码与执行步骤**，按批次分节，做完一批就在本页补一节。

<aside>
📌

批次 A（现状核对）不产生代码，十二项 grep 结论直接写在索引页第三节「批次 A 核对结论」，本页不重复。

</aside>

## C1. main.ts 收敛（卸 Pinia、删 main.css、DEV 句柄调整）

批次 C 一共六项，其中**三项都落在 `src/main.ts` 这一个文件上**，所以合成一步做完，只跑一次 `npm run build`。剩下三项（`LocalStats` 迁移、「已至卷末」统一、首页设置入口）分别在 C2 / C3 / C4。

### 一、先卸包

`package.json` 第 22、23 行的两个依赖要连 `node_modules` 一起清掉，不能只删代码里的 import——留着依赖会让下一个人以为项目在用 Pinia。

```bash
cd ~/Documents/Quire/frontend
npm rm pinia pinia-plugin-persistedstate
```

<aside>
⚠️

**卸包必须在改 `main.ts` 之前或同一次改完**。如果先卸包但 `main.ts` 还 import 着 `pinia`，`npm run build` 会直接报模块找不到；反过来先改文件再卸包则始终是可构建状态。建议顺序：改文件 → 卸包 → build。

</aside>

依据：批次 A 查明 `src/` 下**零 `defineStore`**，`main.ts` 里只是建了个空 store 实例并挂上持久化插件，没有任何 store 使用它。第四节的决策是「P1 用 composable + Repository 承载状态，不建 store」，这两个包属于项目初始化时按模板装进来的。

### 二、src/main.ts 完整替换

**三处改动**：① 删 Pinia 相关四行（import 两行、`createPinia()`、`pinia.use()`）与 `app.use(pinia)`；② 不再 import `./styles/main.css`；③ DEV 块删 `$router`、补挂 `$almanac`。

**没有改动的**：`purgeOrphans` 整段连注释原样保留，位置仍在 `app.mount` 之后（P1-5 页第 11 节定的接入点）；`import * as repo` 仍是静态导入。

```tsx
import "@/styles/index.css"

import { createApp } from "vue"

import App from "./App.vue"
import router from "./router"
// 静态导入整个 repo 命名空间：既给下面的 purgeOrphans 用，
// 也让 DEV 调试句柄不再需要顶层 await import()
import * as repo from "@/repo"

// 拆开链式调用，才能在 mount 之后继续往下写
const app = createApp(App)
app.use(router)
app.mount("#app")

/**
 * 清理孤儿图片。三个刻意的选择：
 *   1. 放在 mount 之后 —— 它跟首屏渲染没有任何关系，不能让它挡在前面
 *   2. 不 await（fire-and-forget）—— 失败了也不该影响应用启动
 *   3. 默认只清 24 小时之前的 —— 正在另一个标签页里编辑、还没保存的图片
 *      entryId 同样是空串。不设这个窗口就会把它们删掉，用户那边表现为
 *      「图片突然变空白」，且完全无法归因。
 */
void repo.mediaRepo
  .purgeOrphans()
  .then((n) => {
    if (import.meta.env.DEV && n > 0) console.info(`[quire] 已清理 ${n} 张孤儿图片`)
  })
  .catch((err) => {
    console.warn("[quire] purgeOrphans 失败", err)
  })

// DEV 调试句柄，生产构建整块摇掉。铁律 10 依赖这两个句柄，P1 阶段保留。
// $router 已删：那是 P1-4 验收组件复用串文 bug 时的一次性工具。
// $almanac 用动态 import 挂：农历库 gzip 98 KB，静态导入会把它拽进 dev 的
// 初始依赖图；也不用顶层 await，那会挂住整个模块求值、拖长 dev 首屏。
if (import.meta.env.DEV) {
  Object.assign(window, { $repo: repo })
  void import("@/shared/almanac").then((m) => {
    Object.assign(window, { $almanac: m.getAlmanac })
  })
}
```

<aside>
💡

**为什么 `$repo` 静态、`$almanac` 动态，不是不一致。** `repo` 无论如何都要在启动时求值——下面的 `purgeOrphans` 就用它，静态导入不额外增加任何成本。农历库不一样：它只有 `HomeView` 需要，Vite 已经把它拆成独立 chunk（索引页第四节「农历库体积」那条决策）。如果为了挂调试句柄而静态 import，dev 下每次启动都会多加载一个 295 KB 未压缩的 chunk，等于自己把已经拆好的 chunk 又粘回来。

</aside>

### 三、删掉 src/styles/main.css

```bash
rm src/styles/main.css
grep -rn "main.css" src/ || echo "无残留引用"
```

定性结论见索引页第八节：它是**早期版本的 `index.css`**，260 字节，重复一遍 `@import "tailwindcss"` 与五个颜色令牌，五个值与 `index.css` 逐字一致，基础重置是 `index.css` 的严格子集。**不是** Vite 模板残留（模板残留会带 `#app { max-width: 1280px; text-align: center }`，那才是会跟 `max-w-2xl` 打架的东西）。

<aside>
⚠️

**删之前确认 `index.css` 里这段在**：

```css
html,
body,
#app {
  height: 100%;
}
```

`main.css` 里也有同样一段，两个文件都提供时删掉一个无感。但如果 `index.css` 里的被误删，症状要到**批次 G** 才出现——吸顶月份不生效、下拉刷新没有触发区域，两者都依赖滚动容器有确定高度。届时几乎不可能追回到「批次 C 删文件时连带删了这行」。

</aside>

### 四、验收

```bash
npm run build
```

1. 构建无类型错误、无「Cannot find module 'pinia'」
2. `grep -rn "pinia" src/ package.json` 应**零命中**
3. `grep -rn "main.css" src/` 应零命中，且 `src/styles/` 下只剩 `index.css`
4. `dist/assets/index-*.css` 的**字节数应比改动前小**——重复的 `@import "tailwindcss"` 去掉了。这比用 `grep -c` 数 preflight 靠谱：压缩后的 CSS 只有一行，`grep -c` 数的是行数不是出现次数，一份两份都打印 1
5. `npm run dev` 打开首页：背景仍是麻纸色 `#F5F0E6`、宜忌卡圆角 6px、卡内文字仍是楷体（这三项证明 `index.css` 的令牌在生效，与 `main.css` 无关）
6. 控制台跑 `$repo` 和 `$almanac("2027-01-01")`，两个都要有返回。`$router` 应为 `undefined`

<aside>
📊

**第 6 条里 `$almanac` 的第一次调用可能返回 `undefined is not a function`**。因为它是动态 import 挂上去的，页面刚加载完的那一瞬间 chunk 可能还没到。等一秒再敲即可。这是动态挂载换来体积收益的代价，可以接受——反之如果改成顶层 await，代价是每次 dev 启动都等农历库下载完才渲染。

</aside>

### 五、批次 H 会用到的字符集枚举

补挂 `$almanac` 的**主要理由不是调试便利**，而是批次 H 的楷体子集化需要先确定字符集，而黄历区要显示的汉字不在源码里——宜忌词条、六十甲子、二十八宿、纳音、神煞、彭祖百忌、十二时辰名、节气节日全在 `lunar-typescript` 的中文字符串表里，读代码穷举不出来。子集漏字不报错，只会在某天首页冒出豆腐块或字体退回默认，可能上线数月后才被碰到。

到批次 H 时在 dev 控制台跑这段，输出直接喂 `pyftsubset --text=`：

```jsx
const chars = new Set()
const d = new Date(2026, 0, 1)

// 400 天 = 一个完整农历年 + 闰月余量
for (let i = 0; i < 400; i++) {
  const s = `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, "0")}-${String(d.getDate()).padStart(2, "0")}`
  const a = window.$almanac(s)
  const all = [
    a.lunarFull, a.lunarShort, a.ganZhi, a.zodiac, a.jieQi ?? "", a.zhiXing,
    a.taiShen, a.xiu, a.xiuLuck, a.naYin, a.chong, a.sha,
    ...a.yi, ...a.ji, ...a.festivals, ...a.pengZu,
    ...a.hours.map((h) => h.label + h.god),
  ]
  for (const v of all) for (const c of v) chars.add(c)
  d.setDate(d.getDate() + 1)
}

console.log(chars.size, [...chars].sort().join(""))
```

**还要另外并进去的字符**：UI 固定文案（第二节命名表的十项、空态「今日无事，也可留白」、占位「今日之事，可入青简」、「已至卷末」、「预简（待刻）尚未开放，暂只能刻已至之日」等）、数字与标点。用户自己写的正文**不能**走子集字体，只能退系统字体——这一点到批次 H 要在设计上明确：楷体只用于黄历区，正文用 `--font-cn-sans`。

## C2. LocalStats 迁移（还铁律 7 的账）

批次 A 第 12 项查到：`src/composables/useStorageUsage.ts` 第 4 行 `import type { LocalStats } from "@/db/statsRepo"`，上层直接伸手进了 `@/db/`。

<aside>
🧾

**这笔账 P1-5 就记下了，还记错了一处。** P1-5 页第 12 节 F 组那个 callout 写的是「`SettingsView.vue` 里 import type ... 走的是 `useStorageUsage.ts`」——措辞把违规点说成了 SettingsView。实际 `SettingsView.vue` 压根没 import 过 `LocalStats`（它只 `useStorageUsage()` 拿返回值，类型靠推断），**唯一直接违规的文件就是 `useStorageUsage.ts`**。修法结论不变，改的文件数也不变，只是别去 SettingsView 里找那一行——找不到。

</aside>

### 一、src/shared/types.ts 追加

追加到文件末尾。它是纯数据形状，不依赖 Dexie、也不依赖 `statsRepo` 的实现，本来就该待在 shared 层。

```tsx
/**
 * 本机数据统计。定义放 shared 层：它只是一个形状，
 * 谁来填（Dexie / 将来的小程序存储 / 云端）与它无关。
 */
export interface LocalStats {
  entries: number
  deleted: number
  tags: number
  media: number
  /** 图片原图字节数之和。navigator.storage.estimate() 不可用时的兜底 */
  mediaBytes: number
}
```

### 二、src/db/statsRepo.ts 完整替换

只有两处变化：删掉本地的 `interface`，改成从 `@/shared/types` import type。`load()` 的实现一字不动。

```tsx
import { db } from "./schema"
import type { LocalStats } from "@/shared/types"

export const localStatsRepo = {
  async load(): Promise<LocalStats> {
    const [entries, deleted, tags, media] = await Promise.all([
      db.entries.where("isDeleted").equals(0).count(),
      db.entries.where("isDeleted").equals(1).count(),
      db.tags.count(),
      db.media.count(),
    ])

    // 用 each 逐条累加，不用 toArray()。
    // toArray() 会把所有原图 Blob 同时留在数组里；each 走游标，
    // 每条用完即可回收，只是免不了逐条反序列化。
    let mediaBytes = 0
    await db.media.each((m) => {
      mediaBytes += m.size || m.blob.size
    })

    return { entries, deleted, tags, media, mediaBytes }
  },
}
```

<aside>
⚠️

**不要在 `statsRepo.ts` 里加一句 `export type { LocalStats }` 做兼容。** 那样两个位置都能 import，下次给统计加字段时必然有人只改一处，类型漂移查起来比现在这点改动成本高得多。迁走就是迁走，旧位置不留门。

</aside>

### 三、src/composables/useStorageUsage.ts 改第 4 行

**只改这一行**，文件其余部分（含那段 `try / catch` 与三种拿不到配额情形的注释）一律不动。

```tsx
// 改前
import type { LocalStats } from "@/db/statsRepo"
// 改后
import type { LocalStats } from "@/shared/types"
```

改完这一段的 import 是：

```tsx
import { computed, ref } from "vue"

import { statsRepo } from "@/repo"
import type { LocalStats } from "@/shared/types"
```

### 四、为什么 repo/index.ts 一行都不用改

迁移前我担心的是「`LocalStats` 移走后 `@/repo` 要不要补一个再导出」。看完 P1-5 第 5、6 两节可以确定**不用**：

| 疑问 | 结论 |
| --- | --- |
| `statsRepo.ts` 还导出别的东西吗 | 只有 `LocalStats` 与 `localStatsRepo` 两个。搬走前者后，它只导出 `localStatsRepo`，`repo/index.ts` 那行 `import { localStatsRepo } from "@/db/statsRepo"` 照旧 |
| 上层 import `@/shared/types` 算不算绕过 repo 层 | 不算。铁律 7 禁的是 `@/db/*`。`ListView.vue` / `TrashView.vue` 早就在直接 import `@/shared/types` 的 `EntryListItem`、`EntryOrder`，这是既定口径 |
| 要不要顺手把 `ImportReport` 也搬走 | **不要**。它定义在 `src/db/backupRepo.ts`，但只有同文件内部与 `SettingsView` 的推断在用，**没有任何一处 import 它**，不构成违规。无收益的改动不做 |

### 五、验收

```bash
npm run build
grep -rn 'from "@/db/' src/ --include="*.vue" --include="*.ts" | grep -v "src/repo/index.ts" || echo "没有绕过 repo 层"
grep -rn "LocalStats" src/
```

1. 构建无类型错误
2. 第二条 grep **应打印「没有绕过 repo 层」**——这是 P1-5 第 12 节 F 组第三条第一次真正干净
3. 第三条应恰好三处命中：`shared/types.ts` 的定义、`statsRepo.ts` 的 import type、`useStorageUsage.ts` 的 import type。**四处就说明留了兼容导出**
4. 打开设置页：在册 / 断简 / 图片 / 本域已用四行数字与改动前完全一致。这是纯类型迁移，运行时行为改变一点都算出错

## C3. 统一「已至卷末」的显示条件

索引页第八节记的那条不一致，两处原文：

| 文件 | 现状 | 效果 |
| --- | --- | --- |
| `TrashView.vue`:35 | `v-else-if="page > 1"`，class `end` | 只在确实翻过卷之后提示，楷体居中 |
| `ListView.vue`:57 | `v-else`，class `hint` | 只要没有下一页就提示，**三篇日记也会说「已至卷末」**，且用的是无字族的 `.hint` |

**定案：把 ListView 对齐到 TrashView**，即采用 `page > 1` 这条软条件。反过来把 TrashView 改成无条件是不行的——P1-5 第 12 节 A 组第 5 条已经把「断简里只有三两篇时**不应**出现『已至卷末』」写成了验收判据并且验过了，推翻它等于让一条已验收的用例作废。

### 一、src/views/ListView.vue 三处改动

<aside>
🚩

**第 1 处是这一项唯一的坑，漏了就是静默失效。** `ListView` 现在的 destructure **没有取 `page`**（TrashView 取了）。如果只改模板条件不补这一行，模板里的 `page` 是 `undefined`，`undefined > 1` 恒为 `false`——「已至卷末」**从此再也不出现**，而且不报错、不告警、build 也过。`useEntryList` 本身是返回 `page` 的（P1-4 页第 4 节），只是这里没接。

</aside>

**1. script 段的 destructure**，补一个 `page`：

```tsx
// 改前
const { keyword, order, items, total, loading, hasMore, reload, loadMore } = useEntryList()
// 改后
const { keyword, order, items, total, page, loading, hasMore, reload, loadMore } = useEntryList()
```

**2. 模板里那一行**，就在「再展一卷」按钮下面：

```jsx
<!-- 改前 -->
<p v-else class="hint">已至卷末</p>

<!-- 改后：与断简页同一条口径。只在确实翻过卷（page > 1）且已无下一页时提示；
     首屏就装得下的几条不需要被告知「到底了」，一眼就能看完，提示只是噪声。 -->
<p v-else-if="page > 1" class="end">已至卷末</p>
```

**3. style scoped 段末尾追加 `.end`**（与 TrashView 的 `.end` 逐字一致）：

```css
.end {
  margin: 20px 0;
  font-family: var(--font-cn-kai);
  font-size: 12px;
  line-height: 1.6;
  text-align: center;
  color: var(--color-ink-faint);
}
```

<aside>
🧷

**别去改 `.hint` 本身。** ListView 里 `.hint` 还在给「检简中……」用，而且它跟 `.count`（右上角「共 N 简」）共用一条 `.count, .hint` 选择器。给它加 `font-family: var(--font-cn-kai)` 会把加载提示和计数文字一起变成楷体。新加一条 `.end` 是唯一无副作用的做法。

</aside>

### 二、TrashView.vue 不动

一行都不改。这一项是让 ListView 对齐它，不是双向妥协。

### 三、验收

把 `useEntryList.ts` 里的 `PAGE_SIZE` 临时改成 `2`，造够五六篇日记，验三种情形：

| 情形 | 期望 |
| --- | --- |
| 条目少于一页（`page === 1` 且无下一页） | **不出现**任何到底提示，列表直接结束 |
| 有下一页 | 出现「再展一卷」按钮，无到底提示 |
| 点到最后一卷 | 按钮消失，出现「已至卷末」，**楷体、居中、12px**，与断简页视觉一致 |
| 搜索只命中一条 | 不出现到底提示（`page` 仍是 1） |

验完把 `PAGE_SIZE` 改回 `20`，再跑 `npm run build`。

## C4. 首页入口：设置放 header 右上，成编留在「近作」行

### 一、先定位置（两个入口，不是一个）

| 入口 | 位置 | 理由 |
| --- | --- | --- |
| **设置** | `&lt;header&gt;` 右侧，与大日期块两端对齐 | 它是**全局入口**，不属于任何一个内容分区。且项目至今没有底部 tab bar，列表页 / 断简页 / 设置页彼此跳转全靠页首文字按钮（P1-4 第 5 节、P1-5 第 8 / 9 节都是这个结构），首页不能是唯一的例外 |
| **成编** | **留在「近作」分区标题行右侧**，不上提到 header | 它的语义是「展开这一区的全部」，与所属分区绑定才读得通。更硬的理由在下面那个 callout |

<aside>
🗂️

**为什么不把「成编」也搬到 header：header 右侧的位置已经被批次 F 预定了。**

批次 F 要往这一行里加两样东西：日期切换的左右箭头（点中间弹日历）、以及置灰的签到按钮「上名青简」。现在把「设置」和「成编」两个文字按钮都塞进去，到 F 就是四样元素抢一行，窄屏上必然要拆。**现在少放一个，比到 F 再拿出来便宜。**

</aside>

### 二、现在那个测试按钮的具体毛病

```jsx
<h2 class="section-title">近作</h2>
<button type="button" class="to-list" @click="router.push('/list')">成编</button>
<ul v-if="recent.length" class="entry-list">
```

1. `&lt;button&gt;` 默认是 `inline-block`，但夹在 `&lt;h2&gt;`（块级）与 `&lt;ul&gt;`（块级）之间，结果是**自己占一整行、左对齐**，跟在标题下面像一条多余的列表项
2. `class="to-list"` 是临时名，项目里无对应样式，现在显示的是**浏览器默认按钮**（系统灰底、圆角、字体不是中文字族），与列表页 / 断简页 / 设置页那套 `.plain` 文字按钮完全不同源
3. `recent` 为空时它照旧显示。简册尚空而旁边摆一个「成编」，点进去又是一句「简册尚空，今日宜落笔」，白跑一趟

### 三、模板两处改动

**1. header 改成两端对齐，右侧放「设置」**

```jsx
<!-- 改前 -->
<header class="mb-5">
  <div class="text-4xl" style="font-family: var(--font-cn-serif)">
    {{ dayNum }}
  </div>
  <div class="mt-1 text-base" style="color: var(--color-ink-soft)">
    {{ monthLabel }} · {{ almanac?.weekday }}
  </div>
</header>

<!-- 改后：外层包一层 flex，日期块内部一字不动 -->
<header class="head mb-5">
  <div>
    <div class="text-4xl" style="font-family: var(--font-cn-serif)">
      {{ dayNum }}
    </div>
    <div class="mt-1 text-base" style="color: var(--color-ink-soft)">
      {{ monthLabel }} · {{ almanac?.weekday }}
    </div>
  </div>
  <button type="button" class="plain" @click="router.push('/settings')">设置</button>
</header>
```

**2.「近作」标题与「成编」排成一行**

```jsx
<!-- 改前 -->
<h2 class="section-title">近作</h2>
<button type="button" class="to-list" @click="router.push('/list')">成编</button>

<!-- 改后 -->
<div class="sec-head">
  <h2 class="section-title">近作</h2>
  <button
    v-if="recent.length"
    type="button"
    class="plain accent"
    @click="router.push('/list')"
  >
    成编
  </button>
</div>
```

### 四、style scoped 追加四条

`.plain` 与 `.accent` 与 ListView / TrashView / SettingsView 的同名规则**逐字一致**（scoped 不能复用，只能拄一份，这是四个视图既定的代价）。

```css
.head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12px;
}

/* baseline 而不是 center：标题与按钮字号不同，对齐基线才不会看着错行 */
.sec-head {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 12px;
}

.plain {
  padding: 6px 2px;
  border: none;
  background: none;
  font-size: 15px;
  line-height: 1.7;
  color: var(--color-ink-soft);
  cursor: pointer;
}

.accent {
  color: var(--color-bamboo);
}
```

旧的 `.to-list` 规则如果已经写进 `&lt;style scoped&gt;`，一并删掉。

<aside>
🎨

**首页字号偏离阶梓这事不在 C4 修。** `text-4xl` 是 36px、`text-base` 是 16px，而品牌板的阶梓是 44 / 28 / 20 / 15 / 12（大日期那个位置对应 `--text-display: 44px`）。列表页与设置页已经全部走令牌，只有 HomeView 还在用 Tailwind 字号类 + 行内 `style`。

改字号是**看得见的视觉变动**，C4 只是搬个按钮，两件事搐在一个提交里，一旦首屏看着不对就分不清是哪一改弄的。**归批次 F**（那一批本来就要重排 header 并做 FAB 样式）。

</aside>

### 五、路由核对结果

`src/router/index.ts` 七条路由加一条 404 catch-all，**C4 不需要改路由**：

- `/settings`、`/list`、`/trash` 都已就位（`/trash` 是 P1-5 第 10 节那步补的，已确认生效）
- 每条路由都有 `name`，但现有视图一律用字符串路径跳转（`router.push('/settings')`）。**保持字符串写法**，不要在 C4 引入 `{ name: "settings" }` 这种第二种风格
- `scrollBehavior: () =&gt; ({ top: 0 })` 无条件置顶：**批次 G 要回来看它**。列表页加上吸顶月份与下拉刷新后，从详情页返回应该回到原滚动位置，而不是跳回顶部——那时要改成 `savedPosition ?? { top: 0 }`。现在不改

<aside>
❗

**发现一条新的文档不一致：`/schedules` 路由已经存在，指向 `@/views/ScheduleView.vue`**。

而 2026-08-21 的定案是「预简 / 待刻」整条推 P4，P1-6 只在日期选择器上加「禁选未来日期」的软限制。重新核对 `App.vue` 后确认：**移动端底栏其实已有 `<RouterLink to="/schedules">预简</RouterLink>`**，所以不是“只能手敲地址”；点它会进入 P0 占位页。

C4 不改它，但批次 F 必须同时处理：路由保留给 P4，底栏入口在 P4 前改成原生 disabled 占位（或移除）。`ScheduleView.vue` 已确认无写库代码，不会造数据，但继续让可点击入口落到占位文字同样是坏体验。

</aside>

### 六、验收

```bash
npm run build
grep -n "to-list" src/views/HomeView.vue || echo "临时按钮已清"
```

1. 首页右上角出现「设置」二字，与大日期同一行、顶部对齐（不是垂直居中——日期块有两行，居中会让按钮掉到半空中）
2. 点「设置」→ 进设置页；设置页左上「今简」→ 退回首页。**双向闭环**，不依赖浏览器后退
3. 首页 → 设置 → 断简 → 后退两次 回到首页，中途不出现空页
4. 「成编」与「近作」同一行、右对齐、竹青色 `#5A7A6B`、无浏览器默认按钮底色
5. **空库态**：清掉所有日记（或新开无痕窗口）→ 「近作」下只有「今日无事，也可留白」，**「成编」按钮不出现**；写一篇后刷新，按钮出现
6. 窄屏（DevTools 调 iPhone SE 375px）下，「设置」不与日期块重叠、不换行

<aside>
📋

**批次 C 六项到此齐了**：C1（main.ts 收敛）、C2（LocalStats 迁移）、C3（已至卷末）、C4（首页入口）。全批验收通过后要回头做两件登记：索引页第八节把「已至卷末两处不一致」标为已解决、并补上本节新发现的 `/schedules` 与首页字号两条；清单页回勾对应 checkbox。

</aside>

## B. media.thumbBlob（缩略图）

### 一、两个待定问题的答案：都不用升

| 问题 | 结论 | 依据 |
| --- | --- | --- |
| 要不要升 Dexie version | **不用** | P1-1 第 5 节 schema 是 `media: "id, entryId, createdAt, dirty"`，`thumbBlob` 不在索引里。Dexie 的 `version()` 管的是**索引声明**，非索引字段是自由形状，直接 `put` 就行 |
| `formatVersion` 要不要 +1 | **不用** | P1-5 第 1 节的 `BackupMedia` 已经有 `thumbBase64: string | null`，第 4 节的导出与导入两条路径也都已经在处理 `thumbBlob`。备份外壳**一字不变**，变的只是这个值从「恒为 null」变成「通常有值」。`formatVersion` 是给结构变更用的，值域变化不算 |

<aside>
🕳️

**批次 B 比原估的还小。** `MediaItem.thumbBlob: Blob | null` 在 **P1-1 第 1 节就已经定义好了**，备份格式也早就留好了位。真正缺的只有一件事：`mediaRepo.add()` 里那行 `thumbBlob: null` **从来没被填过**。这是 P1-1 就挖好、一直空着的坑，不是新开的字段。

</aside>

### 二、两个方向的兼容都得顾

**旧数据进来**：P1-1 到 P1-5 期间贴的图、以及任何旧备份（`thumbBase64: null`）导入后，`thumbBlob` 都是 `null`。所以**读取侧必须容忍 null 并回退原图**，不能假设缩略图一定存在——否则批次 G 的列表缩略图会在旧图上摆一片空白。

**不做启动时批量回填。** 对存量图片逐张解码生成，几百张就是启动时一次长时间占用主线程，而且跟 `purgeOrphans` 抢同一个启动窗口。改成**惰性补生成**：`getThumb()` 发现是 null 就当场生成一张写回去。

### 三、src/capabilities/image.ts（新增）

<aside>
🧱

**为什么放 `capabilities/` 而不是 `shared/`**：缩图要用 `createImageBitmap` 与 canvas，回退路径还要碰 `document.createElement`。铁律 6 要求 `shared/` 纯 TS，而 P1-5 第 3 节建 `capabilities/` 的时候就定了它是「唯一允许摸 document 的地方」。这是该目录的第二个成员。

**签名故意做成通用的 `resizeToBlob`，不是 `makeThumb`**。批次 E 的「图片压缩」需要的是同一个函数，只是 `maxEdge` 大得多（1600 之类）。现在就把参数留出来，E 那一批不用再动这个文件。

</aside>

```tsx
export interface ResizeOptions {
  /** 长边上限（CSS 像素）。短边按原比例缩 */
  maxEdge: number
  quality?: number
  mime?: string
}

/**
 * 把一张图缩到指定长边以内。
 *
 * 返回 null 有两种含义，调用方处理方式一样——「用原图就行」：
 *   1. 原图已经比目标小（不放大：放大只会让文件变大、画质不变）
 *   2. 解码失败（SVG / 动图 / 损坏文件 / 浏览器不支持）
 *
 * 注意它 throw 不出来。缩略图缺失只应降级成用原图，
 * 绝不能让「贴图」这个动作本身失败。
 */
export async function resizeToBlob(
  src: Blob,
  { maxEdge, quality = 0.8, mime = "image/webp" }: ResizeOptions,
): Promise<Blob | null> {
  if (typeof createImageBitmap !== "function") return null

  let bmp: ImageBitmap
  try {
    // imageOrientation 必须显式传。手机拍的竖图靠 EXIF orientation 记旋转，
    // 不传这个选项时部分浏览器会按未旋转的像素解码，
    // 结果是缩略图躺着、原图站着。
    bmp = await createImageBitmap(src, { imageOrientation: "from-image" })
  } catch {
    return null
  }

  try {
    const longEdge = Math.max(bmp.width, bmp.height)
    if (longEdge <= maxEdge) return null

    const scale = maxEdge / longEdge
    const w = Math.max(1, Math.round(bmp.width * scale))
    const h = Math.max(1, Math.round(bmp.height * scale))

    // OffscreenCanvas 不碰 document，优先走它
    if (typeof OffscreenCanvas === "function") {
      const canvas = new OffscreenCanvas(w, h)
      const ctx = canvas.getContext("2d")
      if (!ctx) return null
      ctx.imageSmoothingQuality = "high"
      ctx.drawImage(bmp, 0, 0, w, h)
      return await canvas.convertToBlob({ type: mime, quality })
    }

    // 回退：真实 DOM canvas。只有这一处碰 document，所以整个文件得待在 capabilities/
    const el = document.createElement("canvas")
    el.width = w
    el.height = h
    const ctx = el.getContext("2d")
    if (!ctx) return null
    ctx.imageSmoothingQuality = "high"
    ctx.drawImage(bmp, 0, 0, w, h)

    return await new Promise<Blob | null>((resolve) => {
      el.toBlob((b) => resolve(b), mime, quality)
    })
  } catch {
    return null
  } finally {
    bmp.close()
  }
}
```

<aside>
🔍

**webp 不支持时不会报错。** `convertToBlob` / `toBlob` 遇到不认识的 `type` 会**静默退回 PNG**，于是你拿到一张能用但大得多的缩略图。不必为此加探测（只影响体积，不影响正确性），但验收时若发现 `thumbBlob.type` 是 `image/png`，原因在这里，不是代码写错。

</aside>

### 四、maxEdge 定 480 的算术

两个消费方尺寸不同，取大的那个：

| 消费方 | CSS 尺寸 | 2× DPR 需求 |
| --- | --- | --- |
| 批次 E 九宫格预览 | 容器 `max-w-2xl` = 672px，三列减间距 ≈ **218px** | ≈ **436px** |
| 批次 G 列表缩略图 | ≈ 56–72px | ≈ 144px |

所以 `THUMB_MAX_EDGE = 480`，`quality = 0.8`。480px webp 一张约 30–60 KB，对照原图（手机直出常见 3–5 MB）是两个量级的差别——这正是 P1-4 那句「现在直接读原图 Blob 会让一屏列表吃掉几十 MB 内存」要解的问题。

### 五、src/db/mediaRepo.ts 改两处、加一个方法

`mediaRepo` 早就在用 `createImageBitmap`（`measure()`），本来就不是纯 TS，再多一个 `@/capabilities/image` 依赖不改变分层；铁律 7 禁的是**上层** import `@/db/*`，不管 `db/` 往下依赖。

**1. 顶部追加 import 与常量**

```tsx
import { resizeToBlob } from "@/capabilities/image"

/** 缩略图长边。算术见本节第四小节 */
const THUMB_MAX_EDGE = 480
const THUMB_QUALITY = 0.8
```

**2. `add()` 里把 `thumbBlob: null` 填上**

```tsx
  async add(blob: Blob): Promise<MediaItem> {
    const { width, height } = await measure(blob)
    // 返回 null 就是「不需要缩略图」（图本来就小，或解不了码），
    // 这与旧数据的 null 含义一致，读取侧统一回退原图，不用分两种情形
    const thumbBlob = await resizeToBlob(blob, {
      maxEdge: THUMB_MAX_EDGE,
      quality: THUMB_QUALITY,
    })

    const item: MediaItem = {
      id: newId(),
      entryId: "",
      blob,
      thumbBlob,
      mime: blob.type || "image/png",
      width,
      height,
      size: blob.size,
      sortOrder: 0,
      remoteUrl: "",
      createdAt: utcNow(),
      dirty: 1,
    }
    await db.media.add(item)
    return item
  },
```

**3. 新增 `getThumb()`，带惰性补生成**

```tsx
  /**
   * 取缩略图。永远能拿到一张能显示的图（实在没有才返回 undefined）。
   *
   * 旧图（P1-1~P1-5 期间贴的）与旧备份导入的 thumbBlob 都是 null，
   * 在这里惰性补上。不在启动时批量回填：那等于启动时把全库图片
   * 解码一遍，还要跟 purgeOrphans 抢同一个启动窗口。
   */
  async getThumb(id: string): Promise<Blob | undefined> {
    const m = await db.media.get(id)
    if (!m) return undefined
    if (m.thumbBlob) return m.thumbBlob

    const t = await resizeToBlob(m.blob, {
      maxEdge: THUMB_MAX_EDGE,
      quality: THUMB_QUALITY,
    })
    // 生成不出来就不写库，下次还会再试一次。对「本来就比 480 小」的图
    // 确实是白跑一次 decode，但换来的是不必在库里再存一份几乎等大的副本
    if (t) await db.media.update(id, { thumbBlob: t, dirty: 1 })
    return t ?? m.blob
  },
```

<aside>
⚖️

**一个明知的取舍：`add()` 里是同步 await 生成缩略图的。** 手机直出的 4000px 大图，decode + 缩图可能花几百毫秒，这段时间编辑器里的图还没插入。

选同步是因为它简单且没有竞态——异步补写会出现「先落库为 null、片刻后才有值」，而此间用户可能已经保存并导出了备份。**如果实测卡，不要在批次 B 改**：批次 E 做图片压缩时本来就要重排这条流水线（压缩后的图才是真正要存的原图），到时一并改成「先插占位 → 后台处理」。

</aside>

<aside>
📌

`measure()` 里的 `createImageBitmap(blob)` **没传 `imageOrientation`**，所以带 EXIF 旋转的竖图存下的 `width` / `height` 可能是反的。影响面很小（这两个值目前没有任何消费方），**批次 B 不改**，归批次 E（九宫格要靠它们算宽高比占位，那时才真正要紧）。

</aside>

### 六、验收

```jsx
// 造一张大图（拖一张手机照进编辑器最真实，或用 fetch 拉一张）
const m = await $repo.mediaRepo.add(bigBlob)
console.log(m.thumbBlob?.type, m.thumbBlob?.size, m.blob.size)
// 期望：image/webp（或 image/png，见第三节那个 callout），
//       size 比原图小一两个量级

// 长边确实被限到 480
const bmp = await createImageBitmap(m.thumbBlob)
console.log(Math.max(bmp.width, bmp.height))   // 应为 480
```

1. **小图不生成**：贴一张 200px 的小图 → `thumbBlob` 为 `null`。**这是预期，不是 bug**；`getThumb(id)` 应返回原图（`size` 与 `blob.size` 相等）
2. **惰性补生成**：拿一张 P1-3/P1-5 时期贴的旧图（`thumbBlob` 为 null）跑 `getThumb` → 返回一张比原图小的 Blob；再跑一次应**直接命中已存的那张**（可把 `resizeToBlob` 里临时加 `console.log` 确认只跑了一次）
3. **备份往返**：导出 → 文件里 `media[0].thumbBase64` 是长串而非 `null`；删库 → 导入 → `getThumb` 直接有值（不需重新生成）
4. **旧备份兼容**：手改一份备份，把 `thumbBase64` 全改成 `null`（`formatVersion` **不改**）→ 导入不报错，`getThumb` 惰性补上。**这一条就是「`formatVersion` 不用 +1」的验证**
5. **异常输入**：贴一个 SVG 或故意损坏的文件 → `add()` **不报错**，`thumbBlob` 为 null，图片本身照旧存得进去

```bash
npm run build
grep -rnE '\b(window|document)\.' src/shared/ || echo "shared 层无浏览器全局"
grep -rn 'createElement\|createObjectURL' src/views/ || echo "视图层不碰 DOM 与 objectURL"
```

<aside>
✅

第二条 grep 仍应打印「无浏览器全局」——新增的 `document.createElement` 在 `capabilities/`，不在 `shared/`。第三条也仍应干净，P1-5 第 12 节 F 组那条 grep 只查 `src/views/`，**不会误伤这个新文件**。如果它仮阳了，说明你把 `resizeToBlob` 写进了视图或 shared，位置错了。

</aside>

### 七、批次 B 故意没做的

- **不改 UI**。列表缩略图属批次 G、九宫格属批次 E。B 只把数据准备好，**跑完 B 页面看不出任何变化**——这是正常的，验收全靠控制台
- **不做原图压缩**。那是批次 E，且会改到同一条流水线，两件事分开提交才能分清体积变化是哪一改带来的
- **不动 `remoteUrl` 与 `dirty` 的语义**。缩略图要不要上云、上哪个尺寸，是 P2 同步那一层的设计题
- **不动 Dexie version**。理由见本节第一小节。无事升 version 不是无害的：一旦升了，就得同时维护一段 `upgrade()` 逻辑，而那段逻辑在旧库上只跑一次、几乎无法重复测试

### 六之补、怎么拿到一张可测的大图

<aside>
⚠️

上面第六节那段里的 `bigBlob` **是个占位变量名，不是真存在的东西**，直接粘进控制台必然报 `ReferenceError: bigBlob is not defined`。下面两条路径选一条，**优先路径 A**。

</aside>

**路径 A：端到端，验真实贴图流程（推荐）**

你现在做的就是这个：开「刻一简」、拖一张大图。差的只是**把那一篇存下来**——存之前图是孤儿（`entryId` 为空字符串），而 repo 层没有「列出孤儿图」的方法，拿不到 id。存一下，`attach()` 就把 `entryId` 填上了：

```jsx
// 拖图 → 点保存/返回，确保那一篇落库了，再跑这段
const latest = (await $repo.entryRepo.list({ order: "updatedAtDesc" })).items[0]
const media = await $repo.mediaRepo.listByEntry(latest.id)
console.log(media.length, "张图")

const m = media[0]
console.log(m.mime, m.blob.size, "→", m.thumbBlob?.type, m.thumbBlob?.size ?? "(null)")

if (m.thumbBlob) {
  const bmp = await createImageBitmap(m.thumbBlob)
  console.log("长边", Math.max(bmp.width, bmp.height))   // 应为 480
  bmp.close()
}
```

<aside>
🔍

**路径 A 能多验出一件事：编辑器的贴图处理到底有没有走 `mediaRepo.add()`**。

如果 A 里 `thumbBlob` 是 `null`，但下面路径 B 能正常生成，那就**不是缩略图代码的问题**，而是 P1-3 的贴图 handler 绕过了 `add()`（比如自己拼了一个 `MediaItem` 直接 `put`）。那得回 P1-3 页查贴图那段，而不是改 `capabilities/image.ts`。两条路径都跑一遍就能分开这两种毛病。

</aside>

**路径 B：本地造一张，只验 repo 层**

不联网、无 CORS、尺寸可控：

```jsx
const c = document.createElement("canvas")
c.width = 4000
c.height = 3000
const x = c.getContext("2d")
// 必须画噪点，不能用纯色：纯色 4000×3000 编码后只有几 KB，
// 原图本身就比缩略图还小，「小一两个量级」那条断言会彻底失真
for (let i = 0; i < 4000; i += 4) {
  x.fillStyle = `hsl(${(i * 7) % 360} 70% ${30 + (i % 40)}%)`
  x.fillRect(i, 0, 4, 3000)
}
const bigBlob = await new Promise((r) => c.toBlob(r, "image/jpeg", 0.95))
console.log("造图", bigBlob.type, bigBlob.size)

const m = await $repo.mediaRepo.add(bigBlob)
console.log(m.thumbBlob?.type, m.thumbBlob?.size, m.blob.size)
```

<aside>
🧹

**路径 B 会往库里真的塞一条孤儿图**（`entryId` 为空）。不管它也行，`purgeOrphans` 下次启动过 24 小时自会带走。

想立即清干净只能 `await $repo.mediaRepo.purgeOrphans(0)`，**但这一下会连你刚拖进编辑器、还没保存的那张一起删掉**。确认编辑器里没有未保存的图再跑。

</aside>

两条路径都测不了 EXIF 旋转（canvas 造的图没有 EXIF，而手机照经过编辑器也可能已被处理）。**这是预期的**，方向问题已归批次 E。

## D1. 编辑器：visibilitychange 时 flush

这一改是 P1-3 第 7 节那个黄框自己点名留给 P1-6 的：`beforeunload` 里的保存不保证成功，因为浏览器卸载页面时不等异步操作，而 IndexedDB 写入是异步的。

<aside>
💡

**为什么 `visibilitychange` 能真解决问题，而 `beforeunload` 不能。**

`beforeunload` 触发时页面正在死，异步写入可能执行不完。`visibilitychange`（切后台）触发时**页面还活着**，IndexedDB 写入能正常完成。而且移动端真实丢数据的路径几乎全是「切后台 → 系统回收 WebView」，不是「点刷新」——iOS Safari 在 App 切到后台时**会**发 `visibilitychange`，却常常**不发**完整的 `beforeunload`。

所以这不是「又加一道保险」，而是**在移动端把唯一可靠的那道补上**。

</aside>

### 一、EntryEditView.vue 改一处

找到现在这段（在 `onBeforeRouteLeave` 下面）：

```tsx
/** 关标签页 / 刷新前落盘。注意这里只能同步触发，await 不会被等待 */
function onBeforeUnload(): void {
  flush()
}

window.addEventListener("beforeunload", onBeforeUnload)

onBeforeUnmount(() => {
  window.removeEventListener("beforeunload", onBeforeUnload)
  flush()
})
```

换成：

```tsx
/** 关标签页 / 刷新前落盘。注意这里只能同步触发，await 不会被等待 */
function onBeforeUnload(): void {
  flush()
}

/**
 * 切后台 / 切标签页时落盘。
 *
 * 只在 hidden 时做。visibilitychange 双向触发，回到前台也会发一次，
 * 而回前台时并没有新输入，白跑一次 persist 没意义。
 *
 * 与 beforeunload 不重复：pending 为 false 时 persist 会直接 return，
 * 两个事件都触发也只会写一次库。
 */
function onVisibilityChange(): void {
  if (document.visibilityState === "hidden") flush()
}

window.addEventListener("beforeunload", onBeforeUnload)
document.addEventListener("visibilitychange", onVisibilityChange)

onBeforeUnmount(() => {
  window.removeEventListener("beforeunload", onBeforeUnload)
  document.removeEventListener("visibilitychange", onVisibilityChange)
  flush()
})
```

<aside>
📎

三个容易写错的细节：

1. **监听的是 `document` 不是 `window`**。`visibilitychange` 在 `window` 上也能冒泡到，但规范上它是 `document` 的事件，写 `window` 属于碰巧
2. **必须判 `hidden`**。不判的后果不是报错，而是每次切回页面都多走一次 `persist`（虽然 `pending` 为 false 时会提前 return，但意图不清）
3. **移除监听不能漏**。`EntryEditView` 是路由组件，反复进出会反复注册；漏了就是每进一次编辑器多一个监听，而旧监听里的 `flush` 闭包持有的是**已卸载组件的 `editorRef`**，切后台时会拿旧快照去写库
</aside>

### 二、debounce 仍然留 800ms，不调短

P1-3 那个黄框给了两个选项（调短 debounce / 加 `visibilitychange`），**只做后一个**。调短 debounce 是用“每次都多写库”去换“极少数情况少丢一点”，而 `visibilitychange` 是零成本覆盖真正的丢数据路径。两个一起做只会让 800ms 这个早已验收过的值白白变得可疑。

### 三、验收

1. **切标签页**：编辑器里输入一段字，**不等 800ms** 立即切到另一个标签页 → 右上角状态应已变「已保存」（切回来看）
2. **真机切后台**（这一条是重点，桌面浏览器测不出真价值）：手机访问 `5173`，输入一段字后**立即回桌面 / 切 App**，等十秒再回来刷新页面 → 内容在
3. **不重复写库**：在 `persist()` 开头临时加 `console.log("persist")`，输入一段字等它自动保存完，再切后台一次 → **不应再打印新的 persist**（因为 `pending` 已是 false）
4. **监听无泄漏**：首页 → 编辑器 → 返回，重复三次；第四次进去输入并切后台 → `persist` **只应打印一次**。打印多次就是漏了 `removeEventListener`
5. `npm run build`

## D2. 自动草稿（已定案 2026-08-24：整项推 P2，P1-6 不做）

<aside>
❓

**定案：不做。不新增 `draftRepo`，不改 `repo/index.ts`，不碰 `meta` 表。**

理由不是成本，是它在当前架构下**解决不了它想解决的问题**：

1. **它能覆盖的场景，现有机制已经覆盖了。** `EntryEditView` 有 800ms 自动保存，停手不到一秒内容就真的进 `entries` 表了。一份 3 秒 debounce 的草稿最多救回 3 秒输入，**比已经在跑的 800ms 更差**
2. **它覆盖不了的场景，换存储也救不了。** 真正救不回来的只有浏览器崩溃 / 进程被强杀——那时没有任何事件触发，一行代码都执行不了，`localStorage` 的同步写也没机会被调用。要防这个只能「敲一个字就同步写一次」，而那正是会让打字卡顿的做法
3. **D1 已经堆掉了原本那个缺口。** 关标签页、切后台、切 App，浏览器都会先发一次 `visibilitychange → hidden`，那时页面还活着，写入能正常完成
4. **P1-3 「空日记不落库」也没留下缺口。** 只有标题与正文**都**为空才不落库；而 `insertImages` 会把文件名写进 `alt`，`toPlainText` 会把 alt 算进纯文本——所以连「只贴了图」都会落库，没有需要草稿去接的东西

**P2 再评估。** 接后端后会出现「本地有、服务端没有」的真实中间态，草稿那时才有独立含义（而且那时该叫「待同步」，不是「草稿」）。

以下是当时核源码时记下的两个阻碍，留着给 P2 参考：

1. `meta` 表虽然在 P1-1 第 5 节就建好了（`MetaRow { key, value }`），但 **`repo/index.ts` 没有任何入口暴露它**。铁律 7 下 `EntryEditView` 不能直接碰 `@/db/*`，所以走 `meta` 就必须新增一个 `draftRepo` 并改 `repo/index.ts`
2. 更关键：`meta` 表在 IndexedDB 里，**写入同样是异步的**。用它去兜 P1-3 第 7 节那个「`beforeunload` 异步写不保证完成」的洞，等于用同一根不可靠的管子兜同一个洞
</aside>


## E–I 手工实施总说明（2026-08-26，方案已定、源码未实施）

<aside>
⚠️

**本节及后续 E–I 是按 2026-08-26 当前源码形成的手工实施稿，不代表源码已经完成。**

执行时严格按 **E1 → E2 → E3 → F → G → H → I**。每改完一个文件立刻在 `frontend/` 跑一次 `npm run build`；Python 文件则在 `backend/` 跑对应 smoke / Ruff。不要把多个文件全改完后才第一次构建，否则很难定位是哪一步引入问题。

固定边界：P1 数据仍只落 IndexedDB；上层只 import `@/repo`；`src/shared/` 保持纯 TS；图片正文 `src` 永远保持 `local://media/{id}`；不实现 P4 的未来日程分支；不改 JSON 备份命名。

</aside>

### 一、当前源码核对结论

| 项目 | 2026-08-26 实际源码 | 本批处理 |
| --- | --- | --- |
| 原图压缩 | `mediaRepo.add()` 只生成 480px `thumbBlob`，原图原样落库 | E1 |
| 九宫格 / 排序 | 图片仍是 Tiptap 顶层 `image` 节点；`draggable: true`，但 NodeView 仍是单列原图 | E2 |
| 图片删除后的媒体 | `attach()` 只认领正文里仍存在的 id，不把已从正文删掉的媒体退回孤儿 | E2 修复 |
| 心情 / 天气 / 标签 | Entry 类型和 entryRepo 已支持；编辑页没有 UI；没有 `tagRepo` | E3 |
| 首页日期 | `const today = todayLocal()` 固定；过去日期无法经首页传给编辑器 | F |
| `/schedules` | 路由存在，但 `ScheduleView.vue` 只是静态占位，无写库代码 | F 保持不动 |
| 列表页 | 只有搜索、排序、分页和文字摘要 | G |
| 字体 | 无 woff2、无 `@font-face`、无字体许可证文件 | H |
| README / env | 只有 Vite 模板 README；只有未跟踪的 `.env.development` | I |
| 后端异常 | `main.py` 已有 404 / 422 / 500 三类处理器 | I 只修 422 可序列化缺口 |

---

## E1. 图片压缩：先定最终原图，再测量、再生缩略图

### 一、定案

1. **不引入 `browser-image-compression`。** B 批已经有 `src/capabilities/image.ts::resizeToBlob()`，继续复用，避免第二套 canvas / EXIF 逻辑。
2. 可压缩位图统一尝试输出 WebP：长边 `1920`、质量 `0.8`。
3. GIF 原样保留。把 GIF 画进 canvas 会只剩第一帧，这是数据损坏，不是“体积优化”。
4. `resizeToBlob()` 返回 `null`、解码失败或压缩结果不比原图小时，都保留原 Blob。
5. `mime / size / width / height` 必须描述**最终存进 `MediaItem.blob` 的 Blob**，不能继续描述用户选入的原文件。
6. 480px `thumbBlob` 从最终 Blob 生成，缩图与测量并行；仍会发生原图压缩解码、最终图测量解码、最终图缩略解码三次，E1 真机验收必须关注峰值内存，不能把并行误写成“只解码一次”。

### 二、先把 `collectMediaIds()` 移到 `src/shared/text.ts`

媒体对账、备份导入和编辑保存都需要同一套正文引用解析。它只是纯 JSON 遍历，与同文件 `toPlainText()` 同类，不依赖 Tiptap 运行时：

```tsx
export function collectMediaIds(doc: unknown): string[] {
  const ids: string[] = []

  const walk = (node: unknown): void => {
    if (!node || typeof node !== "object") return
    const value = node as {
      type?: string
      attrs?: { src?: string }
      content?: unknown[]
    }

    if (value.type === "image" && typeof value.attrs?.src === "string") {
      const id = parseLocalSrc(value.attrs.src)
      if (id && !ids.includes(id)) ids.push(id)
    }
    if (Array.isArray(value.content)) value.content.forEach(walk)
  }

  walk(doc)
  return ids
}
```

从 `src/editor/media.ts` 删除旧定义；`EntryEditView.vue` 改为从 `@/shared/text` 同时 import `collectMediaIds / toPlainText`。每个文件改完 build，并确认 shared 没有 Vue / DOM / Dexie / Tiptap import。

### 三、`src/db/mediaRepo.ts`

顶部补 `import { collectMediaIds } from "@/shared/text"`。在缩略图常量上方补原图参数，并把 `measure()` 与 `add()` 换成下面的形状；文件里的 `get / listByEntry / purgeOrphans / getThumb` 此步不动：

```tsx
const IMAGE_MAX_EDGE = 1920
const IMAGE_QUALITY = 0.8
const THUMB_MAX_EDGE = 480
const THUMB_QUALITY = 0.8

async function measure(blob: Blob): Promise<{ width: number | null; height: number | null }> {
  if (typeof createImageBitmap !== "function") return { width: null, height: null }
  try {
    const bmp = await createImageBitmap(blob, { imageOrientation: "from-image" })
    const size = { width: bmp.width, height: bmp.height }
    bmp.close()
    return size
  } catch {
    return { width: null, height: null }
  }
}

async function compressForStorage(blob: Blob): Promise<Blob> {
  // 动图不能经过 canvas，否则只剩第一帧。
  if (blob.type === "image/gif") return blob

  const compressed = await resizeToBlob(blob, {
    maxEdge: IMAGE_MAX_EDGE,
    quality: IMAGE_QUALITY,
    mime: "image/webp",
  })

  // 小图、解码失败、浏览器不支持，或转完反而更大：全部保留原图。
  return compressed && compressed.size < blob.size ? compressed : blob
}
```

`add()` 完整替换为：

```tsx
async add(blob: Blob): Promise<MediaItem> {
  const storedBlob = await compressForStorage(blob)
  const [{ width, height }, thumbBlob] = await Promise.all([
    measure(storedBlob),
    resizeToBlob(storedBlob, {
      maxEdge: THUMB_MAX_EDGE,
      quality: THUMB_QUALITY,
      mime: "image/webp",
    }),
  ])

  const item: MediaItem = {
    id: newId(),
    entryId: "",
    blob: storedBlob,
    thumbBlob,
    mime: storedBlob.type || blob.type || "image/png",
    width,
    height,
    size: storedBlob.size,
    sortOrder: 0,
    remoteUrl: "",
    createdAt: utcNow(),
    dirty: 1,
  }
  await db.media.add(item)
  return item
},
```

同文件此时就新增全库对账，不能等 G 进列表才做。实现只一次取 Entry JSON 与 media 主键；媒体行在事务中逐条 `get`，避免 `db.media.toArray()` 把全库 Blob 同时握进内存：

```tsx
async reconcileAll(): Promise<number> {
  const entries = await db.entries.toArray()
  const mediaIds = (await db.media.orderBy("id").primaryKeys()) as string[]
  const expected = new Map<string, { entryId: string; sortOrder: number }>()

  for (const entry of entries) {
    const ids = collectMediaIds(entry.content?.doc)
    ids.forEach((id, sortOrder) => {
      if (!expected.has(id)) expected.set(id, { entryId: entry.id, sortOrder })
    })
  }

  let changed = 0
  await db.transaction("rw", db.media, async () => {
    for (const id of mediaIds) {
      const item = await db.media.get(id)
      if (!item) continue
      const next = expected.get(id) ?? { entryId: "", sortOrder: 0 }
      if (item.entryId === next.entryId && item.sortOrder === next.sortOrder) continue
      await db.media.update(id, { ...next, dirty: 1 })
      changed += 1
    }
  })
  return changed
},
```

它包含在册与断简 Entry：断简图片仍保持关联，恢复不丢；只有任何正文都不引用的媒体才退回 `entryId=""`。

### 四、`src/main.ts`：启动顺序必须先对账，再 purge

当前 mount 后直接 fire-and-forget `purgeOrphans()`。替换为同一条 Promise 链：

```tsx
void repo.mediaRepo
  .reconcileAll()
  .then((reconciled) =>
    repo.mediaRepo.purgeOrphans().then((purged) => ({ reconciled, purged })),
  )
  .then(({ reconciled, purged }) => {
    if (import.meta.env.DEV && reconciled > 0) {
      console.info(`[quire] 已校正 ${reconciled} 条图片关联`)
    }
    if (import.meta.env.DEV && purged > 0) {
      console.info(`[quire] 已清理 ${purged} 张孤儿图片`)
    }
  })
  .catch((err) => {
    // 对账失败时 then 链不会进入 purge：宁可暂留孤儿，也不能误删正文仍引用的图。
    console.warn("[quire] 图片对账 / 孤儿清理失败", err)
  })
```

仍在 `app.mount` 后、仍不 await，不阻塞首屏；但安全顺序固定为 reconcile → purge。改完 build，并模拟一张 `entryId=""`、createdAt 超 24h、正文仍引用的图：刷新后应先被认领，绝不能被 purge。

<aside>
📌

`resizeToBlob()` 本身仍保持“纯能力函数”：只负责变换，不决定结果是否值得采用。GIF 跳过、无收益回退属于媒体业务规则，放在 `mediaRepo`，不要塞进 `capabilities/image.ts`。

此步不改 Dexie version、不改 `MediaItem`。但 E1 核源码新发现：现有备份只存 `thumbBase64`，导入却拿原图 `mime` 重建缩略 Blob；JPEG 原图 + WebP 缩图会变成“WebP 字节、image/jpeg 类型”。因此下面必须补缩图 MIME，并把备份外壳升到 v2。**这不是推翻 B 的 `thumbBlob` 非索引字段结论，而是 B 当时漏查出的独立备份元数据缺口。**

</aside>

### 五、`src/shared/backup.ts` 与 `src/db/backupRepo.ts`：保存缩略图真实 MIME

`BackupMedia` 增加可选字段，`BACKUP_FORMAT_VERSION` 从 1 升 2：

```tsx
export const BACKUP_FORMAT_VERSION = 2

export interface BackupMedia extends Omit<MediaItem, "blob" | "thumbBlob"> {
  blobBase64: string
  thumbBase64: string | null
  /** v2 新增；可选是为了继续读取 v1 备份 */
  thumbMime?: string | null
}
```

导出 `media.push()` 时补：

```tsx
thumbBase64: thumbBlob ? await blobToBase64(thumbBlob) : null,
thumbMime: thumbBlob?.type || null,
```

导入时不能简单回退 `rest.mime`。v1 没有 `thumbMime`，要按文件头识别：

```tsx
function detectImageMime(b64: string, fallback: string): string {
  const raw = atob(b64.slice(0, 32))
  const bytes = Array.from(raw, (char) => char.charCodeAt(0))

  if (bytes[0] === 0xff && bytes[1] === 0xd8 && bytes[2] === 0xff) return "image/jpeg"
  if (bytes.slice(0, 4).join(",") === "137,80,78,71") return "image/png"

  const head = raw.slice(0, 6)
  if (head === "GIF87a" || head === "GIF89a") return "image/gif"
  if (raw.slice(0, 4) === "RIFF" && raw.slice(8, 12) === "WEBP") return "image/webp"

  return fallback || "application/octet-stream"
}
```

解构和重建改为：

```tsx
const { blobBase64, thumbBase64, thumbMime, ...rest } = bm

const thumbBlob = thumbBase64
  ? await base64ToBlob(
      thumbBase64,
      thumbMime || detectImageMime(thumbBase64, rest.mime),
    )
  : null

// decoded.push 内使用 thumbBlob，不再内联 base64ToBlob(..., rest.mime)
```

同一路径顺手修 `asCopy` 的 Entry / Media 关联。必须在解码 media **之前**先给所有 Entry 建映射：

```tsx
const entryIdMap = new Map<string, string>()
for (const entry of file.entries) {
  entryIdMap.set(entry.id, conflict === "asCopy" ? newId() : entry.id)
}
```

`decoded.push()` 的 media entryId 改为：

```tsx
entryId: rest.entryId
  ? (entryIdMap.get(rest.entryId) ?? rest.entryId)
  : "",
```

构造 incoming Entry 时 id 改为：

```tsx
id: entryIdMap.get(e.id) ?? e.id,
```

不能继续让 Entry 与 Media 各自发新 id 后仍写 `entryId: m.entryId`；那会让复制条目的图继续挂在原条目上，彻底删除原条目时误删副本图片。

顶部从 `./mediaRepo` import `localMediaRepo`。导入 Dexie 事务结束后、`return report` 前执行：

```tsx
// 必须在事务外：按最终落库的 Entry JSON 校正 media.entryId / sortOrder。
await localMediaRepo.reconcileAll()
```

这样导入后立刻去断简彻底删除也不会踩旧关联；不能依赖用户稍后进入列表才对账。

兼容口径：新代码接受 v1，靠文件头恢复 MIME；新导出为 v2；旧代码看到 v2 会明确拒绝而不是静默造错类型。两个文件各改完一次 build；旧备份兼容用真实 v1 文件验，不只手改版本号。

### 六、E1 验收

每测一种图，都先保存日记，再通过 `$repo.mediaRepo.listByEntry(id)` 看真实落库值：

1. 4000×3000 JPEG：`Math.max(width, height) <= 1920`；`blob.size < 原文件.size`；`mime` 与 `blob.type` 一致。
2. 1000px 小图：允许原样保留，不得放大。
3. GIF：`blob.type === "image/gif"`，动画仍会动；`thumbBlob` 允许为 null 并由读取侧回退。
4. 已经高度压缩的 WebP：若新 Blob 更大，应保留原图。
5. 手机竖图：`height > width`，编辑器预览方向正确。
6. 10MB 边界：原文件仍按 E 前已有的 10MB 入口限制判断；不要把“先压后允许超限”偷偷改进口径。
7. v2 导出里 `thumbMime` 等于真实 `thumbBlob.type`；导入后 Blob type 不变。
8. 导入一份无 `thumbMime` 的 v1 备份：WebP / PNG / JPEG 缩图均由文件头恢复正确 type。
9. 真机连续贴 9 张接近 10MB 的图，观察处理期间页面不崩溃；记录峰值内存与耗时，必要时 E1 内再改为串行测量/缩图，不能牺牲正确性。
10. 造一张超 24h、正文仍引用但 `entryId=""` 的图后刷新：启动对账先认领，purge 不得删除。
11. `asCopy` 导入后，新 Entry id、新 media id、media.entryId 与正文 local id 四者一致；立即彻底删除原条目不影响副本图片。
12. 每改完 `shared/text.ts`、`EntryEditView.vue`、`mediaRepo.ts`、`main.ts`、`shared/backup.ts`、`backupRepo.ts` 都立即 `npm run build`。

---

## E2. 九宫格、大图、删除与拖拽排序

### 一、数据结构定案：不加 gallery 节点，不升 schemaVersion

九宫格只是一种**编辑呈现**，正文仍是一串现有 `image` 节点：

```text
Tiptap JSON 中 image 节点顺序
        ↓ 唯一真相
collectMediaIds(doc)
        ↓ 保存时派生
media.sortOrder
```

因此：

- 不新增 `gallery` / `imageGrid` 节点，不升 `CONTENT_SCHEMA_VERSION`。
- 不在 Entry 外再存一份图片顺序。
- 不让 SortableJS 直接搬 ProseMirror DOM；DOM 搬了但 JSON 没搬，会出现“眼前顺序正确，刷新后恢复旧顺序”的静默 bug。
- 拖拽最终必须 dispatch ProseMirror transaction。
- `src` 与 alt 原样保留，永远不把 blob URL 写入 attrs。

### 二、`src/editor/media.ts`：批量连续插图 + transaction 移动命令

`insertImages()` 不要再每插一张就 `createParagraphNear()`。先把所有文件落库，最后一次性插入连续 image 节点，再补一个段落；这样 CSS 才能把连续图片排成九宫格。

核心形状：

```tsx
const nodes: Array<{ type: "image"; attrs: { src: string; alt: string } }> = []

// 原有格式 / 10MB 校验照旧；每个合法文件：
const item = await mediaRepo.add(file)
nodes.push({
  type: "image",
  attrs: { src: toLocalSrc(item.id), alt: file.name },
})
inserted += 1

if (nodes.length) {
  editor
    .chain()
    .focus()
    .insertContent([...nodes, { type: "paragraph" }])
    .run()
}
```

同文件新增移动命令。调用方只传 media id，不缓存 ProseMirror position；position 会随每次 transaction 改变，缓存就是错的：

```tsx
import type { Node as ProseMirrorNode } from "@tiptap/pm/model"

function imageAt(
  doc: ProseMirrorNode,
  mediaId: string,
): { pos: number; node: ProseMirrorNode } | null {
  let found: { pos: number; node: ProseMirrorNode } | null = null

  doc.descendants((node, pos) => {
    if (found || node.type.name !== "image") return
    const id = typeof node.attrs.src === "string" ? parseLocalSrc(node.attrs.src) : null
    if (id === mediaId) found = { pos, node }
  })

  return found
}

export function moveImageNode(
  editor: Editor,
  sourceId: string,
  targetId: string,
  side: "before" | "after",
): boolean {
  if (sourceId === targetId) return false

  const source = imageAt(editor.state.doc, sourceId)
  const target = imageAt(editor.state.doc, targetId)
  if (!source || !target) return false

  const rawTarget = side === "before" ? target.pos : target.pos + target.node.nodeSize
  const tr = editor.state.tr.delete(source.pos, source.pos + source.node.nodeSize)
  const mappedTarget = tr.mapping.map(rawTarget, side === "before" ? -1 : 1)

  tr.insert(mappedTarget, source.node)
  editor.view.dispatch(tr.scrollIntoView())
  return true
}
```

<aside>
⚠️

这里显式从 `@tiptap/pm/model` 只 import type，避免让 shared 层或持久化类型依赖 Tiptap。**不要**为省类型错误改成 `getJSON()` 后数组 splice 再 `setContent()`；`setContent()` 会重建整篇文档，破坏撤销栈与当前选区。

</aside>

改完本文件先 build。

### 三、`collectMediaIds()` 已在 E1 前移

E1 已把该纯 JSON helper 移到 `src/shared/text.ts`，E2 不再重复修改。这里仅确认拖拽后的 doc 仍由同一 helper 按节点顺序抽出 media id。

### 四、`src/db/mediaRepo.ts`：`attach()` 同步顺序并退回已删除媒体

当前实现会让“正文已删、media 仍挂 entryId”的图片永远绕过 `purgeOrphans()`。完整替换：

```tsx
async attach(entryId: string, mediaIds: string[]): Promise<void> {
  await db.transaction("rw", db.media, async () => {
    const keep = new Set(mediaIds)
    const attached = await db.media.where("entryId").equals(entryId).toArray()

    // 不立即物理删除：用户仍可能撤销。退回孤儿后由启动时的
    // purgeOrphans() 按既有 24 小时窗口清理。
    for (const item of attached) {
      if (!keep.has(item.id)) {
        await db.media.update(item.id, { entryId: "", sortOrder: 0, dirty: 1 })
      }
    }

    for (let i = 0; i < mediaIds.length; i++) {
      await db.media.update(mediaIds[i], { entryId, sortOrder: i, dirty: 1 })
    }
  })
},
```

改完本文件 build，并重新跑 E1 的压缩验收一张，防止第二次编辑覆盖 E1。

### 五、`src/editor/LocalImageView.vue`：缩图常驻、原图只在灯箱打开时读

NodeView 的职责定为：

1. 解析 `local://media/{id}`。
2. 格子常驻 `mediaRepo.getThumb(id)`，配合 `useObjectUrl()`；不再常驻原图。
3. 点击格子才 `mediaRepo.get(id)` 取原图给灯箱。
4. 删除调用 `props.deleteNode()`，只删 JSON 节点；**不直接删 media 表**。
5. 拖拽把手用 Pointer Events。按下后记录 source id，抬起时用 `document.elementFromPoint()` 找 `.local-image[data-media-id]`，按落点在目标格左 / 右半区决定 before / after，再调 `moveImageNode()`。
6. 所有控件都 `contenteditable="false"`，并 `stopPropagation()`，避免按钮操作改动编辑器选区。
7. 触屏长按约 350ms 后才进入拖动，普通轻点仍是开大图；桌面鼠标可立即拖。

NodeView wrapper 至少带这些属性：

```vue
<NodeViewWrapper
  class="local-image"
  :class="{ selected, dragging }"
  :data-media-id="mediaId || undefined"
>
  <button v-if="url" type="button" class="preview" contenteditable="false" @click.stop="open">
    <img :src="url" :alt="alt" draggable="false" />
  </button>
  <div v-else-if="missing" class="missing">图片已丢失</div>
  <div v-else class="loading" />

  <div class="image-actions" contenteditable="false">
    <button type="button" aria-label="拖动图片排序" @pointerdown.stop.prevent="startDrag">移</button>
    <button type="button" aria-label="删除图片" @click.stop="props.deleteNode()">删</button>
  </div>
</NodeViewWrapper>
```

异步取缩略图时必须有请求序号，防止 NodeView 被复用后旧 Promise 覆盖新 src：

```tsx
const blob = ref<Blob | null>(null)
const url = useObjectUrl(blob)
let resolveSeq = 0

async function resolve(src: string): Promise<void> {
  const mine = ++resolveSeq
  blob.value = null
  missing.value = false

  const id = parseLocalSrc(src)
  mediaId.value = id ?? ""
  if (!id) return

  const thumb = await mediaRepo.getThumb(id)
  if (mine !== resolveSeq) return
  if (!thumb) missing.value = true
  else blob.value = thumb
}
```

拖拽生命周期不能只写 `startDrag`。NodeView 至少实现下面的激活、移动、结束、取消与卸载清理；移动超过 8px 但长按尚未触发时取消拖拽，让页面正常滚动：

```tsx
let holdTimer: number | null = null
let activePointer: number | null = null
let startX = 0
let startY = 0
let handle: HTMLElement | null = null

function cleanupDrag(): void {
  if (holdTimer !== null) window.clearTimeout(holdTimer)
  holdTimer = null
  window.removeEventListener("pointermove", onPointerMove)
  window.removeEventListener("pointerup", finishDrag)
  window.removeEventListener("pointercancel", cancelDrag)
  if (handle && activePointer !== null && handle.hasPointerCapture(activePointer)) {
    handle.releasePointerCapture(activePointer)
  }
  activePointer = null
  handle = null
  dragging.value = false
}

function activateDrag(): void {
  if (!handle || activePointer === null) return
  try {
    handle.setPointerCapture(activePointer)
    dragging.value = true
  } catch {
    cleanupDrag()
  }
}

function startDrag(event: PointerEvent): void {
  cleanupDrag()
  activePointer = event.pointerId
  startX = event.clientX
  startY = event.clientY
  handle = event.currentTarget as HTMLElement

  window.addEventListener("pointermove", onPointerMove, { passive: false })
  window.addEventListener("pointerup", finishDrag)
  window.addEventListener("pointercancel", cancelDrag)

  if (event.pointerType === "mouse") activateDrag()
  else holdTimer = window.setTimeout(activateDrag, 350)
}

function onPointerMove(event: PointerEvent): void {
  if (event.pointerId !== activePointer) return
  const moved = Math.hypot(event.clientX - startX, event.clientY - startY)
  if (!dragging.value && moved > 8) {
    cleanupDrag()
    return
  }
  if (dragging.value) event.preventDefault()
}

function finishDrag(event: PointerEvent): void {
  if (event.pointerId !== activePointer) return
  if (dragging.value && mediaId.value) {
    const target = document
      .elementFromPoint(event.clientX, event.clientY)
      ?.closest<HTMLElement>(".local-image[data-media-id]")
    const targetId = target?.dataset.mediaId
    if (target && targetId) {
      const rect = target.getBoundingClientRect()
      const side = event.clientX < rect.left + rect.width / 2 ? "before" : "after"
      moveImageNode(props.editor, mediaId.value, targetId, side)
    }
  }
  cleanupDrag()
}

function cancelDrag(): void {
  cleanupDrag()
}
```

原图灯箱也要有独立请求序号：

```tsx
const fullBlob = ref<Blob | null>(null)
const lightboxOpen = ref(false)
let fullSeq = 0

async function open(): Promise<void> {
  if (!mediaId.value) return
  const mine = ++fullSeq
  const item = await mediaRepo.get(mediaId.value)
  if (mine !== fullSeq || !item) return
  fullBlob.value = item.blob
  lightboxOpen.value = true
}

function close(): void {
  fullSeq += 1
  lightboxOpen.value = false
  fullBlob.value = null
}

onUnmounted(() => {
  resolveSeq += 1
  fullSeq += 1
  cleanupDrag()
})
```

模板末尾接 `<ImageLightbox :open="lightboxOpen" :blob="fullBlob" :alt="alt" @close="close" />`。拖拽把手加 `touch-action: manipulation`；只有激活后才 preventDefault。这样短滑仍滚页面，长按拖动不会遗留全局监听。

### 六、新增 `src/components/ImageLightbox.vue`

输入 `blob / alt / open`，内部用 `useObjectUrl()`；关闭方式同时支持背景点击、右上按钮与 Escape。灯箱只存在于打开期间，关闭即 revoke 原图 URL。不要把 URL emit 给父组件长期保存。

最低接口：

```tsx
const props = defineProps<{ open: boolean; blob: Blob | null; alt: string }>()
const emit = defineEmits<{ close: [] }>()
const source = toRef(props, "blob")
const url = useObjectUrl(source)
```

灯箱根节点使用 `role="dialog" aria-modal="true"`；图片 `max-width / max-height` 受视口约束；`@click.self` 关闭。改完新增文件先 build，再接入 NodeView 并再次 build。

### 七、`src/components/DiaryEditor.vue` 非 scoped 样式：只让顶层图片占三列

```css
.prose-diary {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.prose-diary > * {
  flex: 0 0 100%;
  min-width: 0;
}

.prose-diary > .local-image {
  flex: 0 0 calc((100% - 16px) / 3);
  margin: 0;
  aspect-ratio: 1;
}
```

NodeView 内部图片用 `width: 100%; height: 100%; object-fit: cover`。只有**连续的顶层 image 节点**会排成九宫格；普通段落、标题、引用、列表仍独占一行。不要给所有 descendant image 套这条规则。

### 八、E2 验收矩阵

1. 一次选 9 张：出现 3×3；JSON 中是 9 个连续 `image` 节点 + 一个 paragraph。
2. 刷新：顺序与保存前一致，每个 attrs.src 仍是 `local://media/...`。
3. 桌面鼠标拖、手机触摸拖各测一次；拖后 `Ctrl/Cmd+Z` 能撤销，重做能恢复。
4. 保存后 `listByEntry()` 的 id 顺序等于 `collectMediaIds(doc)`。
5. 删除一图后保存：该 media 的 `entryId` 变 `""`；撤销后再保存可重新认领；没有立即丢 Blob。
6. 灯箱关闭、离开编辑页后 Object URL 都被 revoke；反复开关时内存不持续上涨。
7. 旧日记单图、图片夹在两段正文中都可正常编辑，不强行迁移 JSON。
8. 缺失 media 显示“图片已丢失”，不会抛未捕获异常。

---

## E3. 心情、天气与标签录入

### 一、选项唯一来源：新增 `src/shared/entryMeta.ts`

这是纯常量，允许放 shared；不 import Vue / DOM / Dexie / Tiptap。

```tsx
export interface EntryMetaOption {
  value: string
  label: string
}

export const MOOD_OPTIONS: readonly EntryMetaOption[] = [
  { value: "😊", label: "欣然" },
  { value: "😌", label: "安然" },
  { value: "🥰", label: "欢喜" },
  { value: "🤔", label: "沉思" },
  { value: "😴", label: "疲倦" },
  { value: "😔", label: "低落" },
  { value: "😢", label: "难过" },
  { value: "😤", label: "烦闷" },
] as const

export const WEATHER_OPTIONS: readonly EntryMetaOption[] = [
  { value: "☀️", label: "晴" },
  { value: "🌤️", label: "少云" },
  { value: "☁️", label: "阴" },
  { value: "🌫️", label: "雾" },
  { value: "🌦️", label: "阵雨" },
  { value: "🌧️", label: "雨" },
  { value: "⛈️", label: "雷雨" },
  { value: "❄️", label: "雪" },
] as const
```

E 与 G 都只从这里读，不在两个视图各抄一份。改完 build，并跑 shared 纯 TS grep。

### 二、新增 `src/shared/tags.ts`，统一标签归一化

```tsx
/** 只用于比较 / 去重；展示名仍保留首次创建时的 trim 后文本。 */
export function normalizeTagName(name: string): string {
  return name.trim().toLowerCase()
}
```

必须使用无 locale 的 `toLowerCase()`，不能在 tagRepo 用 `toLocaleLowerCase()`、备份导入又用另一套。`src/db/backupRepo.ts` 的 `tagByName` 和 incoming tag key 都改调 `normalizeTagName()`；tagRepo 也调用同一个 helper。两个文件各自 build，并测试 ASCII 大小写、首尾空格、土耳其语系统 locale 下结果一致。

### 三、新增 `src/db/tagRepo.ts`

```tsx
import { db } from "./schema"
import { newId } from "@/shared/ids"
import { normalizeTagName } from "@/shared/tags"
import { utcNow } from "@/shared/time"
import type { Tag } from "@/shared/types"

export interface ITagRepo {
  list(): Promise<Tag[]>
  getMany(ids: readonly string[]): Promise<Tag[]>
  getOrCreate(name: string): Promise<Tag>
}

export const localTagRepo: ITagRepo = {
  async list() {
    const rows = await db.tags.toArray()
    return rows.sort((a, b) => a.name.localeCompare(b.name, "zh-CN"))
  },

  async getMany(ids) {
    const rows = await db.tags.bulkGet([...ids])
    return rows.filter((row): row is Tag => Boolean(row))
  },

  async getOrCreate(name) {
    const normalized = name.trim()
    if (!normalized) throw new Error("标签不能为空")
    if ([...normalized].length > 32) throw new Error("标签不能超过 32 个字符")

    return db.transaction("rw", db.tags, async () => {
      const wanted = normalizeTagName(normalized)
      const rows = await db.tags.toArray()
      const existed = rows.find((tag) => normalizeTagName(tag.name) === wanted)
      if (existed) return existed

      const tag: Tag = {
        id: newId(),
        name: normalized,
        color: null,
        createdAt: utcNow(),
        dirty: 1,
      }
      await db.tags.add(tag)
      return tag
    })
  },
}
```

P1 标签量很小，事务内扫描可接受；tagRepo 与备份导入现在实际共用 `normalizeTagName()`。此时不为 normalizedName 升 Dexie version。

改完新增文件 build。

### 四、`src/repo/index.ts` 暴露 tagRepo

```tsx
import { localTagRepo, type ITagRepo } from "@/db/tagRepo"
// ...原 import

export const tagRepo: ITagRepo = localTagRepo
```

上层的 Meta 组件只能 `import { tagRepo } from "@/repo"`。改完 build，再跑铁律 7 grep。

### 五、新增 `src/components/EntryMetaFields.vue`

接口定为受控组件：

```tsx
const props = defineProps<{
  mood: string | null
  weather: string | null
  tagIds: string[]
}>()

const emit = defineEmits<{
  "update:mood": [string | null]
  "update:weather": [string | null]
  "update:tagIds": [string[]]
  change: []
}>()
```

行为：

- 心情、天气各 8 个 chip；再次点已选项即清空。
- `onMounted()` 调 `tagRepo.list()`。
- 标签多选；输入框 Enter 调 `tagRepo.getOrCreate()`，成功后加入候选并选中。
- `tagIds` 去重；每次变化 emit 对应 update，再 emit `change`，让父页复用现有 800ms `schedule()`。
- 错误（空、超过 32 字）在组件内显示，不用 alert。
- 标签按钮用 tag id 做 key，不用 name；显示名保留用户首次创建时的大小写。

### 六、`src/views/EntryEditView.vue` 接入保存路径

新增状态：

```tsx
const mood = ref<string | null>(null)
const weather = ref<string | null>(null)
const tagIds = ref<string[]>([])
```

`reset()` 同时清空三者；`load(id)` 读取旧文时赋值：

```tsx
mood.value = entry.mood
weather.value = entry.weather
tagIds.value = [...entry.tagIds]
```

标题与编辑器之间插入受控组件：

```vue
<EntryMetaFields
  v-model:mood="mood"
  v-model:weather="weather"
  v-model:tag-ids="tagIds"
  @change="schedule"
/>
```

`persist()` 在 await 前一起快照：

```tsx
const targetMood = mood.value
const targetWeather = weather.value
const targetTagIds = [...tagIds.value]
```

空日记判断必须把元数据算进去，否则“只记今日心情”会被丢掉：

```tsx
const hasMeta = Boolean(targetMood || targetWeather || targetTagIds.length)
if (targetId === null && !targetTitle.trim() && !text.trim() && !hasMeta) {
  pending = false
  status.value = ""
  return
}
```

payload 改成：

```tsx
const payload = {
  title: targetTitle,
  content,
  entryDate: targetDate,
  mood: targetMood,
  weather: targetWeather,
  tagIds: targetTagIds,
}
```

### 七、同文件修保存重入：single-flight + edit revision

当前 `persist()` 可并发：较早保存完成后无条件 `pending=false`，会吞掉保存期间继续输入的修改；新建页两个调用都快照 `targetId=null` 时还可能重复 create。E3 增加串行队列和单调版本：

```tsx
let editRevision = 0
let savedRevision = 0
let persistQueue: Promise<void> = Promise.resolve()

function schedule(): void {
  editRevision += 1
  pending = true
  status.value = "未保存"

  if (timer !== null) window.clearTimeout(timer)
  timer = window.setTimeout(() => {
    timer = null
    void persist().catch(() => {
      status.value = "保存失败"
    })
  }, 800)
}

function persist(): Promise<void> {
  // 每次都链到上一轮 settle 后；catch 只用于让队列可继续，
  // 当前调用仍返回 run，让 flush / 路由守卫能感知失败。
  const run = persistQueue.then(() => persistLatest())
  persistQueue = run.catch(() => undefined)
  return run
}
```

把原 `async function persist()` 改名 `persistLatest()`。开头不能再只看 `pending`，而是：

```tsx
async function persistLatest(): Promise<void> {
  if (savedRevision >= editRevision) return

  // 在真正轮到本任务执行时取最新 revision 与最新表单快照。
  // 队列中的旧请求会自然合并到最新内容，不重复写过期快照。
  const revision = editRevision
  const doc = editorRef.value?.snapshot() ?? loadedDoc.value ?? {
    type: "doc" as const,
    content: [{ type: "paragraph" }],
  }

  // 后续沿用现有 targetId/title/date/content/空日记/payload/create-update/attach，
  // 并加入 E3 的 mood/weather/tagIds 快照。
```

空日记不落库分支不要直接写 `pending=false`，改为：

```tsx
savedRevision = Math.max(savedRevision, revision)
pending = savedRevision < editRevision
status.value = pending ? "未保存" : ""
return
```

`await mediaRepo.attach(...)` 后删掉原来的无条件 `pending=false`，改为：

```tsx
savedRevision = Math.max(savedRevision, revision)
pending = savedRevision < editRevision

// 保存期间若又有输入，editRevision 已更大，状态必须保持“未保存”，
// 后续 timer / flush 会在同一队列继续 update，绝不能被旧保存清掉。
if (entryId.value === savedId) {
  status.value = pending ? "未保存" : "已保存"
}
```

`flush()` 必须排空到调用期间出现的最新 revision：

```tsx
async function flush(): Promise<void> {
  if (timer !== null) {
    window.clearTimeout(timer)
    timer = null
  }

  do {
    await persist()
  } while (savedRevision < editRevision)
}
```

`reset()` 只会在 `await flush()` 后执行；其中再把 `editRevision / savedRevision` 置 0，并把 `persistQueue = Promise.resolve()`。这样新条目首轮一定 create，保存中继续输入的下一轮一定看到已有 `entryId` 并 update，不会重复建两篇。

验收必须人工制造慢保存：在 `entryRepo.create/update` 前临时 delay 1 秒，输入 A 触发保存后立即继续输入 B；最终正文必须含 AB、只建一篇、状态最后“已保存”。保存过程中立刻返回 / 切条目，`flush()` 必须等最新 revision 落库。

<aside>
🧹

当前 `schedule()` 的 timer 内还留着 D1 验收用的 `console.log("persist")`。E3 修改本文件时一并删掉，避免最终提交把调试日志带进去；D1 的功能代码与监听不删。

</aside>

改完本文件 build。E3 验收：新建、编辑、刷新、同路由切另一篇都不串 mood/weather/tagIds；创建 `  Travel  ` 后再建 `travel` 只得到一个标签；导出导入后标签和选择关系不丢。

---

## F. 首页：日期闭环、简片纹、签到占位与 FAB

### 一、`src/shared/time.ts` 新增本地日期 helper

```tsx
export function isLocalDate(value: string): value is LocalDate {
  if (!/^\d{4}-\d{2}-\d{2}$/.test(value)) return false
  const [y, m, d] = value.split("-").map(Number)
  const date = new Date(y, m - 1, d, 12)
  return (
    date.getFullYear() === y &&
    date.getMonth() === m - 1 &&
    date.getDate() === d
  )
}

export function addLocalDays(value: LocalDate, amount: number): LocalDate {
  if (!isLocalDate(value)) throw new Error(`invalid LocalDate: ${value}`)
  const [y, m, d] = value.split("-").map(Number)
  const date = new Date(y, m - 1, d, 12)
  date.setDate(date.getDate() + amount)
  return todayLocal(date)
}

export function isFutureLocalDate(value: LocalDate, today = todayLocal()): boolean {
  return value > today
}
```

用本地中午避开某些时区的夏令时午夜跳变；最终仍经 `todayLocal()` 格式化。禁止出现 `toISOString().slice(0, 10)`。改完 build + shared grep。

### 二、新增 `src/components/HomeDateNavigator.vue`

组件输入 `modelValue / max`，输出 `update:modelValue` 与 `blocked`。结构：左箭头、可点击的当前日期、右箭头；中间覆盖原生 `<input type="date">`。右箭头在 `modelValue >= max` 时原生 disabled。

change 时即使浏览器已设 `:max="max"`，仍二次判断：

```tsx
function choose(value: string): void {
  if (!isLocalDate(value)) return
  if (value > props.max) {
    emit("blocked")
    return
  }
  emit("update:modelValue", value)
}

function step(days: number): void {
  const next = addLocalDays(props.modelValue, days)
  if (next > props.max) {
    emit("blocked")
    return
  }
  emit("update:modelValue", next)
}
```

不要只依赖 `showPicker()`；它不是所有 Safari 版本都有。主交互用可见的原生 date input，能 `showPicker` 时只作增强。

### 三、`src/views/EntryEditView.vue` 接住补记 query

这是 E3 build 通过后对同一文件的**第二次聚焦修改**：先完成并验证元数据保存，再接日期 query，避免两类问题混在一起。`load("new")` 时读取 `route.query.date`：

```tsx
const requested = route.query.date
if (typeof requested === "string" && isLocalDate(requested) && requested <= todayLocal()) {
  entryDate = requested
} else {
  entryDate = todayLocal()
}
```

编辑已有日记时仍以数据库里的 `entry.entryDate` 为准，query 不得覆盖。未来 / 非法 query 回退今天，不写 `schedules`。

### 四、新增 `src/components/BambooSlipEmpty.vue`

用内联 SVG 或纯 CSS 画 3–5 片错落竹简，仅作装饰：根节点 `aria-hidden="true"`，不把文案画进 SVG。颜色只用 `--color-bamboo-soft / --color-paper-deep / --color-ink-faint`，不新增图片请求。

文案仍由 HomeView 输出“今日无事，也可留白”，以后字体子集与无障碍都能正常读到。

### 五、`src/views/HomeView.vue` 数据逻辑重排

核心状态：

```tsx
const today = todayLocal()
const selectedDate = ref(today)
const almanac = ref<AlmanacDay | null>(null)
const dateEntries = ref<EntryListItem[]>([])
const recent = ref<EntryListItem[]>([])
const monthDays = ref(0)
const notice = ref("")
let loadSeq = 0
```

`load()` 必须带请求序号，快速连点日期时旧请求不能盖回新日期：

```tsx
async function load(): Promise<void> {
  const mine = ++loadSeq
  const date = selectedDate.value
  const nextAlmanac = getAlmanac(date)

  const [minePage, latest] = await Promise.all([
    entryRepo.list({ dateFrom: date, dateTo: date, pageSize: 50 }),
    entryRepo.list({ pageSize: 5, order: "entryDateDesc" }),
  ])

  const prefix = date.slice(0, 7)
  const counts = await entryRepo.countByDate(`${prefix}-01`, `${prefix}-31`)
  if (mine !== loadSeq) return

  almanac.value = nextAlmanac
  dateEntries.value = minePage.items
  recent.value = latest.items
  monthDays.value = Object.keys(counts).length
}

watch(selectedDate, () => void load())
```

写作入口把日期带过去：

```tsx
function write(): void {
  void router.push({ path: "/entry/new", query: { date: selectedDate.value } })
}
```

模板调整：

1. header 内接 `HomeDateNavigator`；`@blocked` 显示“预简（待刻）尚未开放，暂只能刻已至之日”。
2. `AlmanacCard` 加 `:key="selectedDate"`，切日后完整黄历展开态应重置。
3. 所选日无条目时显示 `BambooSlipEmpty` + 空态文案；不要再把空态绑在 `recent.length === 0` 上。
4. 当 `selectedDate === today` 用“今日已记”，过去日期用“当日已记 / 补记一简”。
5. 新增真正的 disabled 按钮：主文案“上名青简”，辅助文案或 title“登录后可用”。本批不写任何 repo / meta / API。
6. “近作 / 成编”区继续独立存在。
7. 原全宽 `.write-btn` 改为 FAB；按钮可见文字或 `aria-label` 必须保留。

FAB 移动端需避开 `App.vue` 底栏：

```css
.write-fab {
  position: fixed;
  right: max(20px, calc((100vw - 672px) / 2 + 20px));
  bottom: calc(72px + env(safe-area-inset-bottom));
  z-index: 20;
  min-width: 52px;
  min-height: 52px;
  border: none;
  border-radius: 999px;
  background: var(--color-bamboo);
  color: #fff;
  font-family: var(--font-cn-serif);
  font-size: var(--text-body);
  line-height: var(--leading-body);
  box-shadow: 0 6px 18px color-mix(in srgb, var(--color-ink) 18%, transparent);
}

@media (min-width: 768px) {
  .write-fab { bottom: 28px; }
}
```

### 六、F 同步完成字号归档

HomeView 当前仍有 36 / 19 / 17 / 16 / 14 等非品牌阶梯值。F 改 header/FAB 时一次清完：

- 日期数字 → `--text-display / --leading-display`
- 月份星期、正文、按钮 → `--text-body / --leading-body`
- 分区标题 / 条目标题 → 视层级选 `--text-section` 或 `--text-body`
- 日期、页脚、辅助说明 → `--text-caption / --leading-caption`

不要改全局 token；不要顺手动 DiaryEditor，那属于另一个回归面。

### 七、`src/App.vue`：P4 前禁用移动端“预简”入口

当前底栏确实有可点击的 `/schedules` RouterLink。路由保留，底栏第三项改成不可点击文本：

```vue
<span class="nav-disabled" aria-disabled="true" title="预简（待刻）尚未开放">预简</span>
```

样式与其他导航同尺寸，但使用 `--color-ink-faint`、`cursor: not-allowed`；不能用 `@click.prevent` 假装 disabled，也不能继续跳到 `HomeView5` 占位页。P4 实现日程时再换回 RouterLink。改完 build，并在 375px 点按确认 URL 不变。

### 八、F 验收

月末、年末、闰日、今天右箭头、手工改 DOM 输入未来日期、快速连续切 5 天、过去日期点 FAB 后落库日期、空态插画、签到按钮零写库、375px FAB 不挡底栏、1440px FAB 不漂到屏幕最右。全部通过后 build。

---

## G. 列表页视觉与筛选（硬依赖 E3）

### 一、筛选语义先冻结

- 标签多选采用 **AND**：一篇必须同时包含全部已选标签。UI 辅助文案写“同时包含”。
- 图片筛选是三态：`undefined = 全部`、`true = 有图`、`false = 无图`。
- 日期起止都包含边界；`dateFrom > dateTo` 时不发查询，直接在栏内提示。
- 所有筛选必须在 repo 的排序与分页之前完成。禁止在 ListView 对当前 20 条 `items` 二次 filter。
- “日记本视图”本批只是原生 disabled 占位；不要伪造第二套视图。
- 虚拟滚动仍不做。

### 二、`src/shared/types.ts` 修改 `EntryListParams`

删单数 `tagId?: string`，换成：

```tsx
tagIds?: string[]
dateFrom?: LocalDate
dateTo?: LocalDate
/** undefined 全部；true 有图；false 无图 */
hasImage?: boolean
```

当前没有上层使用旧 `tagId`，无需兼容两种形状。改完 build。

### 三、`src/db/entryRepo.ts`：分页前完成多标签与图片筛选

多标签：

```tsx
if (params.tagIds?.length) {
  const wanted = [...new Set(params.tagIds)]
  coll = coll.and((entry) => wanted.every((id) => entry.tagIds.includes(id)))
}
```

图片条件同样在 collection 阶段完成，但**以正文引用为真相**，不能信任历史 `media.entryId`：E2 上线前删过图片但没重存的旧日记可能仍有脏关联。`entryRepo.ts` 从纯 TS 的 `@/shared/text` import `collectMediaIds`，然后：

```tsx
if (params.hasImage !== undefined) {
  coll = coll.and(
    (entry) => (collectMediaIds(entry.content?.doc).length > 0) === params.hasImage,
  )
}
```

这一步发生在 `toArray / sort / slice` 之前，不会出现分页后剔除。个人日记量级可接受；P2 由服务端媒体关联 / JSON 派生字段接管。改完 build，并用 3 篇（有图 / 无图 / E2 前已删图但 media 仍脏关联）直接调 repo 验结果。

### 四、`src/db/mediaRepo.ts`：批量首图 + 历史关联对账

顶部从 `@/shared/text` import `collectMediaIds`。首图也按正文 JSON 的第一条有效引用，不按可能过期的 `media.sortOrder`：

```tsx
async firstByEntries(entryIds: readonly string[]): Promise<Record<string, string>> {
  if (!entryIds.length) return {}

  const entries = await db.entries.bulkGet([...entryIds])
  const refs = new Map<string, string[]>()

  for (const entry of entries) {
    if (!entry) continue
    const ids = collectMediaIds(entry.content?.doc)
    refs.set(entry.id, ids)
  }

  // 只取主键判断存在，不 bulkGet MediaItem；后者会把原图 Blob 一起解码进内存。
  const existing = new Set(
    (await db.media.orderBy("id").primaryKeys()) as string[],
  )
  const result: Record<string, string> = {}

  for (const [entryId, ids] of refs) {
    const first = ids.find((id) => existing.has(id))
    if (first) result[entryId] = first
  }
  return result
},
```

`reconcileAll()` 已在 E1 与启动清理顺序一起落地，G 不再实现第二份，也不在每次列表刷新全库扫描。列表的首图读取只取 Entry JSON 与 media 主键，避免任何全量 Blob 载入。

该方法与 import 改完 build。

### 五、新增 `src/components/EntryThumbnail.vue`

输入 `mediaId`；内部用请求序号取 `mediaRepo.getThumb()`，Blob 交给 `useObjectUrl()`。id 变化与组件卸载都能 revoke；缺图显示麻纸色占位，不显示浏览器破图图标。

```tsx
const props = defineProps<{ mediaId: string | null }>()
const blob = ref<Blob | null>(null)
const url = useObjectUrl(blob)
let seq = 0

watch(
  () => props.mediaId,
  async (id) => {
    const mine = ++seq
    blob.value = null
    if (!id) return
    const next = await mediaRepo.getThumb(id)
    if (mine === seq) blob.value = next ?? null
  },
  { immediate: true },
)
```

改完新增文件 build。

### 六、`src/composables/useEntryList.ts` 扩筛选状态

新增：

```tsx
const tagIds = ref<string[]>([])
const dateFrom = ref("")
const dateTo = ref("")
const hasImage = ref<boolean | undefined>(undefined)
const filterError = ref("")
```

`fetchPage()` 的第一段先取消旧请求，再 guard 反向日期；错误时清空旧结果，不能一边显示“日期有误”一边保留上一组结果让人误认：

```tsx
async function fetchPage(target: number, append: boolean): Promise<void> {
  const mine = ++seq
  const from = dateFrom.value || undefined
  const to = dateTo.value || undefined

  if (from && to && from > to) {
    filterError.value = "起始日期不能晚于结束日期"
    items.value = []
    total.value = 0
    page.value = 1
    loading.value = false
    return
  }

  filterError.value = ""
  loading.value = true
  // 后续原有 try/finally；repo 参数使用 from / to，并传 tagIds / hasImage
}
```

有效查询完整传给 repo：空日期转 undefined，tagIds 克隆。watch 策略：关键词保留 250ms；order 与三个筛选变化立即 `reload()`。数组用 `watch(tagIds, ..., { deep: true })`。`onScopeDispose()` 清 keyword timer，避免离开列表后旧 timer 仍触发查询。

返回值补四个筛选状态和 `filterError`。ListView 在筛选栏显示它；TrashView 仍只 destructure 旧字段，不受影响。

### 七、`src/composables/usePullToRefresh.ts`（新增）

只在 `window.scrollY <= 0`、单指、纵向下拉、非 loading 时累计距离；阈值建议 64px，视觉位移用阻尼 `Math.min(96, delta * 0.45)`。touchend 超阈值只调用一次传入的 `refresh()`，finally 归零。

P1 的“刷新”语义是重新读取 IndexedDB（也能接住另一个标签页刚写入的内容），不是网络同步。不要显示“同步云端”。组件卸载时清状态 / listener。

### 八、`src/views/ListView.vue` 重排

1. 页头下放“列表视图（active） / 日记本视图（disabled）”。disabled 必须是真 `disabled`，并有 title“后续版本开放”。
2. 搜索下加可折叠筛选栏：标签多选、起止日期、全部/有图/无图、清除筛选。标签来自 `tagRepo.list()`，名称 map 供卡片显示。
3. 首次 `loading && !items.length` 显示 5 条 skeleton；筛选刷新时保留旧结果，只在筛选栏显示小型“检简中”，避免整页闪白。
4. `watch(items)` 后一次调 `mediaRepo.firstByEntries(items.map(id))`，带序号写入 `coverByEntry`。
5. 卡片显示：首图缩略图（若有）、日期、标题、两行摘要、mood、标签名。weather 可在空间允许时显示，但 G 的硬要求是标签与心情。
6. 月标题：

```css
.group-title {
  position: sticky;
  top: 0;
  z-index: 4;
  padding: 6px 0;
  background: color-mix(in srgb, var(--color-paper) 94%, transparent);
  backdrop-filter: blur(6px);
}
```

7. `updatedAtDesc` 不是按日期连续排列，同一个月可能分裂成多段。定案：该排序下只显示一个“近改动”标题，不做月份吸顶；另外两种 entryDate 排序才按月分组。
8. 根容器接下拉刷新位移和提示；不要改成内部 `overflow:auto` 滚动容器，继续让 document 滚动，才能复用 vue-router `savedPosition`。
9. 保留现有 `page > 1` 的“已至卷末”口径。

### 九、`src/router/index.ts` 恢复列表滚动位置

```tsx
scrollBehavior: (_to, _from, savedPosition) => savedPosition ?? { top: 0 },
```

不要再固定返回 `{ top: 0 }`。改完 build。

### 十、G 验收矩阵

至少造 45 篇、跨 3 月、同日多篇、A/B/AB 三种标签组合、有图/无图、各种 mood：

- A+B 只命中 AB（AND 语义）。
- 起始日期晚于结束日期：不调用 repo，旧 items 被清空，栏内显示错误；修正日期后自动恢复查询。
- keyword + 日期 + 标签 + 图片组合后，`total / hasMore / 卷末` 都是完整过滤结果，不是当前页结果。
- E2 前已经从正文删除、但 media 仍挂 entryId 的历史记录：应用启动对账后“有图”筛选和封面都正确，旧媒体退回孤儿；无需重新编辑或先进入列表。
- 旧图 `thumbBlob=null` 惰性补；小图回退原图；无图不留破图空位。
- 封面跟随 E2 排序后的第一张。
- 反复筛选/翻页/进详情/返回，Object URL 创建与 revoke 最终配平，内存不持续涨。
- 滚到第 2 页进详情，再浏览器后退，回到原位置。
- 375px / 1440px 验 sticky、不遮筛选栏；下拉不足阈值不刷新，超过阈值只刷新一次。
- `prefers-reduced-motion` 下 skeleton 不闪烁动画。

---

## H. 楷体 / 手写体 woff2 子集化与弱网回填

### 一、授权先于技术

推荐两套均采用 SIL Open Font License 1.1 的字体：

- 楷体替代：**LXGW WenKai / 霞鹜文楷 Regular**。
- 手写装饰：**Ma Shan Zheng / 马善政毛笔楷书**。

实施前必须从项目官方发布源下载，记录版本 / commit / 下载地址，并把原始 `OFL.txt` 随字体资产归档。不得从 macOS 的 Kaiti SC、Windows 的 KaiTi 抽取后分发。若所选版本声明 Reserved Font Name，子集产物按 OFL 要求改名为项目内部名（下文使用 `Quire Kai Subset` / `Quire Hand Subset`）。

手写体只给固定装饰文案使用：F 的 HomeView 空态文案 `<p class="empty empty-hand">今日无事，也可留白</p>` 明确加 `.empty-hand { font-family: var(--font-cn-hand); }`；可选再用于固定 Slogan，但每新增一句都必须先并入 `hand-chars.txt`。用户标题、正文、自建标签永远走 `--font-cn-sans`，否则任意生僻字都会漏字。**没有这个实际 class 就不算手写体已接入。**

### 二、冻结字符集

先完成 E–G 并冻结 UI 文案，再执行 C1 第五节已有的 `$almanac` 枚举。不要只跑单个 400 天窗口：分别从 2026、2030、2034 年 1 月 1 日各跑 400 天，合并字符集并比较差集；连续两个窗口不再新增字符后才冻结。输出保存为：

```text
tools/fonts/kai-chars.txt
```

另外合并：E–I 全部固定中文文案、0–9、ASCII、中文标点、全角括号、`— · … / + -`。手写体单独使用更小的：

```text
tools/fonts/hand-chars.txt
```

它只含真实使用手写体的固定短句，不能拿楷体全集去做第二份。

### 三、生成目录与命令

建议结构：

```text
frontend/
├─ src/assets/fonts/
│  ├─ quire-kai-subset.woff2
│  ├─ quire-hand-subset.woff2
│  └─ LICENSES/
│     ├─ LXGW-WenKai-OFL.txt
│     └─ Ma-Shan-Zheng-OFL.txt
└─ tools/fonts/
   ├─ README.md
   ├─ kai-chars.txt
   └─ hand-chars.txt
```

用当前 conda 工具环境的 `pyftsubset`；在 `tools/fonts/README.md` 固定当前已核对的 `fonttools==4.51.0`、`brotli==1.0.9`，并记录原字体文件 SHA-256。`pyftsubset` 本身没有 `--version` 参数，版本用 `python -c 'from importlib.metadata import version; print(version("fonttools"))'` 读取。命令形状：

```bash
pyftsubset /path/to/LXGWWenKai-Regular.ttf \
  --text-file=tools/fonts/kai-chars.txt \
  --flavor=woff2 \
  --layout-features='*' \
  --output-file=/tmp/quire-kai-raw.woff2

python tools/fonts/rename_font.py \
  /tmp/quire-kai-raw.woff2 src/assets/fonts/quire-kai-subset.woff2 \
  "Quire Kai Subset" "QuireKaiSubset-Regular"

pyftsubset /path/to/MaShanZheng-Regular.ttf \
  --text-file=tools/fonts/hand-chars.txt \
  --flavor=woff2 \
  --layout-features='*' \
  --output-file=/tmp/quire-hand-raw.woff2

python tools/fonts/rename_font.py \
  /tmp/quire-hand-raw.woff2 src/assets/fonts/quire-hand-subset.woff2 \
  "Quire Hand Subset" "QuireHandSubset-Regular"
```

仅改输出文件名或 CSS family **不会**修改字体内部 name table。新增 `tools/fonts/rename_font.py`：

```python
from pathlib import Path
import sys

from fontTools.ttLib import TTFont

source = Path(sys.argv[1])
target = Path(sys.argv[2])
family = sys.argv[3]
postscript = sys.argv[4]
font = TTFont(source)
name = font["name"]
values = {
    1: family,
    2: "Regular",
    3: f"{family} Regular",
    4: f"{family} Regular",
    6: postscript,
    16: family,
    17: "Regular",
}

for record in list(name.names):
    value = values.get(record.nameID)
    if value is not None:
        name.setName(
            value,
            record.nameID,
            record.platformID,
            record.platEncID,
            record.langID,
        )

target.parent.mkdir(parents=True, exist_ok=True)
font.save(target)
```

生成后用 fontTools 列出 name IDs 1/4/6/16，确认内部名称全部是 Quire 项目名，且不再含原字体 Reserved Font Name；否则产物不得提交。原始 TTF 可不入仓，但许可证、字符表、版本、哈希和复现命令必须入仓。

### 四、`src/styles/index.css`

在 `@theme` 前声明：

```css
@font-face {
  font-family: "Quire Kai Subset";
  src: url("../assets/fonts/quire-kai-subset.woff2") format("woff2");
  font-style: normal;
  font-weight: 400;
  font-display: swap;
}

@font-face {
  font-family: "Quire Hand Subset";
  src: url("../assets/fonts/quire-hand-subset.woff2") format("woff2");
  font-style: normal;
  font-weight: 400;
  font-display: swap;
}
```

令牌改为本地字体优先、系统回退仍在：

```css
--font-cn-kai: "Quire Kai Subset", "Kaiti SC", "STKaiti", KaiTi, "Noto Serif SC", serif;
--font-cn-hand: "Quire Hand Subset", "Kaiti SC", "STKaiti", KaiTi, cursive;
```

不 preload 手写体；浏览器只在实际出现对应元素时请求。`font-display: swap` 保证弱网先显示系统回退，不出现 FOIT。改完 build。

### 五、字符完整性与弱网验收

1. 用 fontTools 读取 woff2 cmap，断言 `kai-chars.txt` / `hand-chars.txt` 每个字符都存在；不能只肉眼看今天。
2. 读取 name table，确认 Family / Full / PostScript Name 都已改为 Quire 内部名，不含受保留原名。
3. 空库首页网络面板应实际请求 hand woff2，空态短句命中 `Quire Hand Subset`；有数据、空态不渲染时不应无条件 preload 手写体。
4. 抽测闰月、节气、传统节日、完整黄历、十二时辰、E–G 新文案。
5. DevTools 禁缓存，Fast 3G / Slow 3G 各跑冷启 3 次取中位数；真机弱网再跑一次。记录 FCP、LCP、可交互时间、字体请求体积、是否 FOIT、字体失败时是否仍可读。
6. 冷启首页与二次访问分开记录；编辑器 chunk 不应被首页提前加载。
7. 当前路由已全是动态 import，不要为了字体手配 `manualChunks`。
8. 将结果回填索引页第四节“农历库体积”：只有真机数据证明首页不可接受，才重新讨论农历库动态策略；不能只因多了字体就先改 chunk。

---

## I. README、env、后端异常处理与最终收尾

### 一、README 定案：根目录新建 `README.md`

仓库是单仓双目录，权威 README 应放项目根；现有 `frontend/README.md` 是 Vite 模板，最终删除或改成一句“见根 README”，不要保留两份启动口径。

根 README 至少包括：

1. 项目定位与 P1 数据只存浏览器 IndexedDB（后端五表 P2 才接）。
2. 目录：`frontend/`、`backend/`、`doc/`。
3. 前置：Node/npm、conda 环境 `quire`（Python 3.12）、MySQL 8.0；不用 Docker / Redis。
4. 后端：`conda activate quire`、复制 `.env.example` 为 `.env`、填 MYSQL/JWT、`python -m alembic upgrade head`、`python -m uvicorn app.main:app --reload --port 8000`。
5. 前端：明确统一使用 npm（当前实际 `package-lock.json` 在更新），`npm install`、`npm run dev`、`npm run build`；5173 的 `/api` proxy 到 8000。
6. 验证：`/api/v1/health`、`/docs`。
7. MySQL 必需值：utf8mb4、UTC、ngram=2；不复制真实密码。
8. 备份 / 恢复入口“防蠹”；清浏览器数据会丢 IndexedDB。
9. 字体来源、OFL 与子集重建文档入口。
10. 离线边界：页面已加载后本地流程可用；当前没有 Service Worker，首次离线打开 / 强刷不保证可用。

README 还必须覆盖 fresh clone。根目录新增 `environment.yml`，按 2026-08-26 本机 `quire` 环境已核对版本固定后端依赖：

```yaml
name: quire
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - fastapi==0.141.1
      - uvicorn[standard]==0.52.3
      - sqlalchemy==2.0.52
      - asyncmy==0.2.14
      - alembic==1.19.1
      - pydantic==2.13.4
      - pydantic-settings==2.15.0
      - python-jose[cryptography]==3.5.0
      - passlib[bcrypt]==1.7.4
      - uuid-utils==0.17.0
      - redis==8.1.0
      - httpx==0.28.1
      - pytest==9.1.1
      - ruff==0.16.3
```

README 新增首次安装 `conda env create -f environment.yml`，已有环境用 `conda env update -f environment.yml --prune`。这样不能只写“activate 一个预先存在的环境”。创建/更新该文件不涉及前端 build；用新环境至少跑 Alembic、异常 smoke 与 uvicorn 启动。

### 二、前端 env 与 `.gitignore`

两个文件都只放无秘密的相对路径：

```dotenv
# frontend/.env.development
VITE_API_BASE_URL=/api/v1
```

```dotenv
# frontend/.env.production
VITE_API_BASE_URL=/api/v1
```

根 `.gitignore` 当前 `.env.*` 全忽略，必须在 `!.env.example` 后加精确白名单：

```gitignore
!frontend/.env.development
!frontend/.env.production
```

不要放真实 host / token / 密码。改完 env 与 gitignore 后用 `git check-ignore -v` 确认两个文件已不再被忽略；前端 build。

### 三、后端 `.env.example`

当前文件名误写为 `backend/.env.exampl`，内容还有 `DEBUG=flase`。删除误拼文件，新增可跟踪的 `backend/.env.example`：

```dotenv
APP_NAME=quire-api
DEBUG=false
API_V1_PREFIX=/api/v1
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=quire
MYSQL_PASSWORD=replace-me
MYSQL_DB=quire
REDIS_ENABLED=false
JWT_SECRET=replace-with-a-long-random-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=120
REFRESH_TOKEN_EXPIRE_DAYS=30
CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]
```

示例只能是假值。真正 `backend/.env` 继续被忽略。

### 四、后端全局异常处理器：不是新增，而是补 422 序列化缺口

`backend/app/main.py` 已经有三类 handler：Starlette HTTP、RequestValidationError、Exception。不要重复注册第二套。已发现的真实缺口是：Pydantic 自定义 validator 抛 `ValueError` 时，`exc.errors()` 的 `ctx` 可能含 ValueError 对象，直接交 `JSONResponse` 会再次序列化失败，本来 422 变成 500。

顶部补：

```python
from fastapi.encoders import jsonable_encoder
```

422 handler 改为：

```python
@app.exception_handler(RequestValidationError)
async def validation_exc_handler(_: Request, exc: RequestValidationError):
    return JSONResponse(
        status_code=422,
        content=jsonable_encoder(
            {"code": 422, "message": "参数校验失败", "data": exc.errors()}
        ),
    )
```

404/HTTP 与 500 handler 保持现有统一体。I 的要求是全局**异常**处理，不顺手把 health 成功响应强改成 `{code,message,data}`，避免扩大 P0 已验收接口的回归面。

新增 `backend/tests/test_exception_handlers.py`，测试路由只在 pytest 进程注册，绝不能写进生产 `app/main.py`：

```python
import os

os.environ.setdefault("MYSQL_PASSWORD", "test-only")
os.environ.setdefault("JWT_SECRET", "test-only-secret")

from fastapi import HTTPException  # noqa: E402
from fastapi.testclient import TestClient  # noqa: E402
from pydantic import BaseModel, field_validator  # noqa: E402

from app.main import app  # noqa: E402


class ValidationBody(BaseModel):
    value: str

    @field_validator("value")
    @classmethod
    def reject_bad(cls, value: str) -> str:
        if value == "bad":
            raise ValueError("bad value")
        return value


@app.get("/_test/http-exception")
async def raise_http_route():
    raise HTTPException(status_code=409, detail="conflict")


@app.post("/_test/validation-exception")
async def raise_validation_route(_: ValidationBody):
    return {"ok": True}


@app.get("/_test/runtime-exception")
async def raise_runtime_route():
    raise RuntimeError("boom")


client = TestClient(app, raise_server_exceptions=False)


def assert_shape(response, status: int) -> None:
    assert response.status_code == status
    assert set(response.json()) == {"code", "message", "data"}
    assert response.json()["code"] == status


def test_global_exception_shapes() -> None:
    assert_shape(client.get("/_test/missing"), 404)
    assert_shape(client.get("/_test/http-exception"), 409)
    assert_shape(
        client.post("/_test/validation-exception", json={"value": "bad"}),
        422,
    )
    assert_shape(client.get("/_test/runtime-exception"), 500)
```

执行：

```bash
cd ~/Documents/Quire/backend
conda activate quire
python -m pytest tests/test_exception_handlers.py -q
python -m ruff check app tests
```

ValueError 那条必须仍是 422；它专门覆盖 `ctx` 含异常对象的回归。测试文件被 uvicorn 启动路径完全忽略，不会给生产 app 留故障路由。

### 五、最终清理核对项（发现即单独一小步处理）

1. `src/api/request.ts` 仍使用 `qingjian_access_token`，与索引已定的 `quire_access_token` 不一致。P2 才启用，但 I 应统一两处 get/remove。
2. `EntryEditView.vue` 的 `console.log("persist")` 在 E3 已要求删；最终 grep 再确认。
3. `src/style.css`、`HelloWorld.vue`、`assets/vite.svg / vue.svg / hero.png` 是未引用模板资产；先 grep 证实零引用，再删。每删一个文件不需要 build，但一组删完必须 build。
4. 当前同时有 `package-lock.json` 与 `pnpm-lock.yaml`。README 已定 npm，则确认团队不再用 pnpm 后删旧 `pnpm-lock.yaml`；不要长期维护双锁文件。
5. 不碰 `purgeOrphans` 启动调用，不删 dev 下 `$repo / $almanac`。
6. 不实现自动草稿，不碰 `meta`，不删 `/schedules` 路由。

### 六、I 与 P1 最终验收

```bash
# frontend
npm run build
grep -rn 'from "@/db/' src/ --include='*.ts' --include='*.vue' | grep -v 'src/repo/index.ts'
grep -rnE '\b(window|document)\.' src/shared/ || true
grep -rn 'toISOString().slice' src/ || true
grep -rn 'qingjian_access_token\|console.log("persist")' src/ || true

# backend
grep -rn 'datetime.now()' app/ || true
python -m uvicorn app.main:app --port 8000
```

最后走一遍 P1 总闭环：断网状态（页面已加载）下看宜忌 → 选过去日期补记 → 带 3 张图、2 个标签、心情天气 → 拖图排序 → 列表按日期+标签+有图筛到 → 进详情返回恢复滚动 → 编辑 → 删除 → 断简恢复 → 防蠹导出。再测 375px / 1440px、首屏弱网、10MB 图、一天 20 条、emoji 标题。

<aside>
✅

只有上面全部实测完成，索引页 E–I 才能从“方案已编制、源码待实施”改为“已完成”。本次写入文档不能提前打完成勾。

</aside>
