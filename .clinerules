# ============================================================
# 青简 / Quire 项目规则
# 适用：当前工作区，尤其是 diary-web / diary-api（后续可改名 qingjian-web / qingjian-api）
# ============================================================

## 项目定位

- 项目名：**青简（Quire / QINGJIAN）**
- 产品定位：私密而郑重的个人编年史日记应用。
- 核心隐喻：三片竹简被一道弧绳编连成册，一天天日记装订成书。
- 主 Slogan：**一编青简，半生浮沉**
- 功能 Slogan：**一日一简，编年成书**

## 当前开发阶段

- 当前工作区主要处于 **P0 骨架** 与 **P1 本地离线闭环** 阶段。
- P0 目标：前端可运行、后端可运行、数据库可连接、健康检查接口可通，先不写业务逻辑。
- P1 目标：不登录即可完成“写、看、改、删、回收站、JSON 导入导出”的本地闭环，数据保存在 IndexedDB。
- P1 阶段严格保持 **本地优先、离线可用、不接登录、不实现账号体系**。

## 通用开发原则

- 优先最小修改；不修改无关代码，不做无关重构，不改变现有目录结构。
- 保持仓库已有风格、命名和模块边界；如果发现设计问题，先说明建议，不擅自重构。
- 写入或修改文件前，先简要说明要改什么、为什么改。
- 任务完成后，说明修改了哪些文件、为什么修改、以及是否还有需要用户手动完成的步骤。
- 无法完成时，明确说明原因、缺少什么、建议如何处理；不猜测、不编造结果。

## Git 与文件安全

- 允许执行只读命令：`git status`、`git diff`、`git log`、`git ls-files` 等。
- 禁止在未得到用户明确要求时执行：
  - `git reset`、`git clean`、`git restore`、`git checkout`、`git switch`
  - `git push`、`git push --force`、`git rebase`
  - `git commit`、`git stash`
- 不删除、不移动、不覆盖用户已有文件；不修改与当前任务无关的配置文件、Docker、CI/CD。
- 已存在的用户修改不得被回退；如与任务相关，要与其共存，不能擅自还原。

## 依赖与环境

- 不要自动安装任何依赖，包括但不限于 `pip install`、`uv add`、`poetry add`、`npm install`、`pnpm add`、`yarn add`。
- 如果缺少依赖、工具或运行时，先告诉用户，等待用户决定。
- 不自动修改 `.env` 中的数据库连接、密钥或环境变量；缺失时提示用户，不自行猜测或补全。
- 不在日志中打印完整数据库连接串、密码、Token 或敏感密钥。

## 数据库与迁移安全

- 数据库统一为 **MySQL 8.0**，字符集必须使用 `utf8mb4`，推荐排序规则 `utf8mb4_0900_ai_ci`。
- 不自动修改数据库结构、数据或索引，不自动执行修复。
- 禁止在未得到用户明确要求时执行：
  - `alembic revision`、`alembic upgrade`、`alembic downgrade`
  - 任何创建、迁移、删除数据库对象的命令
- 如需迁移或修改 schema，先列出影响范围并等待用户明确同意。

## 技术架构约定

### 前端

- 技术栈：Vue 3 + TypeScript + Vite。
- 状态与请求：Pinia、vue-router、axios、dayjs。
- 样式：Tailwind CSS 或 UnoCSS；设计 token 必须映射青简品牌色、字号、圆角、间距。
- P1 本地存储：Dexie + IndexedDB，所有记录客户端生成 **UUID v7**，禁止自增 ID。
- 本地记录统一包含 `createdAt`、`updatedAt`、`clientUpdatedAt`、`deletedAt`。
- 封装 Repository 层，例如 `src/db/entryRepo.ts`，方法签名尽量与后端 API 对齐，为 P2 云端切换做准备。
- 图片以 Blob 存储；读取使用 `URL.createObjectURL`，组件卸载时 `revokeObjectURL`，防止内存泄漏。
- 编辑器使用 Tiptap；`content` 存 Tiptap JSON，并保留 `schemaVersion`，便于后续迁移。

### 后端

- 技术栈：FastAPI + SQLAlchemy 2.0 async + asyncmy + Alembic + Pydantic v2 + Redis。
- 目录目标：
  - `app/main.py`：FastAPI 实例、CORS、异常处理
  - `app/core/`：config / security / deps
  - `app/models/`：SQLAlchemy ORM
  - `app/schemas/`：Pydantic v2
  - `app/api/v1/`：auth / entries / schedules / tags 等路由
  - `app/services/`：业务逻辑
  - `app/db/`：session / base
- 统一响应结构：`{ code, message, data }`。
- 统一分页参数：`page`、`page_size`、`total`。
- 提供 `/health` 健康检查和可访问的 Swagger 文档 `/docs`。

### MySQL 8.0 关键约定

- 原 PostgreSQL 的 `JSONB` 对应 MySQL `JSON`。
- 中文全文检索必须使用 `FULLTEXT ... WITH PARSER ngram`，并配置 `ngram_token_size=2`。
- UUID 主键使用 `BINARY(16)`，写入时使用 `UUID_TO_BIN(uuid, 1)` 交换时间位，避免随机写放大。
- 时间统一使用 `DATETIME(3)` 存 UTC，展示时由应用按 `Asia/Shanghai` 转换。
- JSON 字段不能直接建索引，需要时通过生成列加索引。
- 软删除下的唯一约束，使用 `is_deleted` 或删除状态生成列参与唯一键。

## P1 功能边界

- 首页：今日日期、星期、农历、干支、节气、宜忌、冲煞；提供前后日期切换与日历选择。
- 新建/编辑：标题、日期、富文本内容、图片、标签、心情、天气；支持自动草稿与离开拦截。
- 日期分支：
  - 今天或更早：写入日记。
  - 未来日期：写入 schedule，状态为 pending，暂不直接转日记。
- 列表：按年/月/日分组，支持标签、日期区间、有无图片筛选和本地关键词搜索。
- 回收站与设置：软删除恢复、彻底删除、本地数据统计、JSON 全量备份与导入。
- 签到、日记本视图等登录依赖能力在 P1 保持置灰或标注“登录后可用”。

## 品牌与视觉约束

### 色彩

- 主色·竹青：`#5A7A6B`
- 背景·麻纸：`#F5F0E6`
- 墨色·正文：`#2E2C28`
- 点缀·宜：`#6B8E5A`
- 点缀·忌：`#A64B3C`

建议比例：麻纸约 52%，墨色约 18%，竹青约 16%，苔绿约 9%，酽红约 5%。

### 字体

- 标题：思源宋体 / Noto Serif SC。
- 正文与 UI：系统无衬线，如 PingFang SC / Hiragino Sans GB。
- 手写与仪式感场景：楷体 / Kaiti，正式产品中需要子集化。
- 不使用大红大金堆砌，避免“黄历 App”气质。

### 标志

- Logo 采用“三简一绳”结构：
  - 三根竖线 x 坐标为 `32 / 50 / 68`，中简最高、右简次之、左简最低。
  - 弧绳从左端点 `(24,53)` 经 `(50,45)` 到 `(76,51)`，保持手写流动感。
- 禁止加粗线宽、旋转变形、替换品牌色、添加投影。
- 应用时保留安全留白，最小可用尺寸按导航、封面等场景控制。

### 功能命名与文案

- 首页/今日：今简
- 新建日记：刻一简
- 列表/简册：成编
- 签到：上名青简
- 导出 PDF/成册：杀青
- 全量备份：防蠹
- 日程提醒：预简
- 回收站：断简
- 年度回顾：岁编 / 汗青

文案语气克制、有仪式感，但不要在所有导航中堆砌古词；日常导航优先使用大白话。

## 交付前检查

- 优先保持 P1 本地闭环可运行，刷新浏览器后数据不丢。
- 前端检查 375px 移动端与 1440px 桌面端，避免文字溢出、控件重叠或按钮尺寸漂移。
- 若启动开发服务器，确认端口冲突时换端口，并把可访问地址告诉用户。
- 如未能运行测试或验证，必须在最终回复中明确说明。
